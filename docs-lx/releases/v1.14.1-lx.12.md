The root-owned copy that the macOS `lxd` service runs is now named `/Library/PrivilegedHelperTools/sing-box-lxd` instead of the service label, so `pgrep sing-box` and `ps -c` find the daemon. Everything else is as in lx.11.

Root-owned копия, которую исполняет служба `lxd` на macOS, теперь называется `/Library/PrivilegedHelperTools/sing-box-lxd`, а не ярлыком службы: `pgrep sing-box` и `ps -c` находят демон. Остальное — как в lx.11.

---

<details open>
<summary><h2>🇬🇧 English</h2></summary>

## 🔧 Changed

### 🔤 The service's root-owned copy is `sing-box-lxd`

- Was (lx.11): `/Library/PrivilegedHelperTools/com.leadaxe.sing-box-lxd` with `com.leadaxe.sing-box-lxd.install.json`. `pgrep`, `pkill` and `ps -c` match the process name, which macOS truncates to 16 characters: `com.leadaxe.sing` — no `sing-box` in it.
- Now: `/Library/PrivilegedHelperTools/sing-box-lxd` with `sing-box-lxd.install.json` (same fields). The name fits whole and contains `sing-box`: `pgrep`, `pkill` and `ps -c` find the daemon without `-f`.
- Unchanged: the service label, the plist and `XPC_SERVICE_NAME` (`com.leadaxe.sing-box-lxd`), `--exec-dir`, the ownership check, `install`/`copy`/`status`/`uninstall`/`--keep-copy`, the exit codes, `/admin/info`.
- A copy installed by lx.11 is not touched. `--service=status` reports a plist that runs it as `MISMATCH` (exit 2). Fix it with `sudo sing-box lxd --service=install`; remove the old file by hand. ([SPEC 100](https://github.com/Leadaxe/sing-box-lx/blob/lx/SPECS/TASKS/100-LXD_ROOT_OWNED_BINARY/SPEC.md))

</details>

<details open>
<summary><h2>🇷🇺 Русский</h2></summary>

## 🔧 Изменено

### 🔤 Root-owned копия службы — `sing-box-lxd`

- Было (lx.11): `/Library/PrivilegedHelperTools/com.leadaxe.sing-box-lxd` и `com.leadaxe.sing-box-lxd.install.json`. `pgrep`, `pkill` и `ps -c` сверяют имя процесса, которое macOS усекает до 16 символов: `com.leadaxe.sing` — без `sing-box`.
- Стало: `/Library/PrivilegedHelperTools/sing-box-lxd` и `sing-box-lxd.install.json` (поля те же). Имя влезает целиком и содержит `sing-box`: `pgrep`, `pkill` и `ps -c` находят демон без `-f`.
- Не меняются: ярлык службы, plist и `XPC_SERVICE_NAME` (`com.leadaxe.sing-box-lxd`), `--exec-dir`, проверка владельца, `install`/`copy`/`status`/`uninstall`/`--keep-copy`, коды выхода, `/admin/info`.
- Копия, поставленная lx.11, не трогается. `--service=status` показывает plist, который её исполняет, как `MISMATCH` (выход 2). Исправить: `sudo sing-box lxd --service=install`; старый файл удалить руками. ([SPEC 100](https://github.com/Leadaxe/sing-box-lx/blob/lx/SPECS/TASKS/100-LXD_ROOT_OWNED_BINARY/SPEC.md))

</details>
