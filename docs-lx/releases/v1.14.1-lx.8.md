The gRPC transport now understands Xray's custom-path form of `service_name`. A value with a leading `/` — `"/xxx/something/Tun"` — is sent as that exact path, the way Xray itself builds it; before, the whole value was escaped as one segment and the server answered `v2ray-grpc: unexpected status: 404 Not Found`. Values without a leading `/` behave byte for byte as before. Not yet verified against a live Xray server — the wire path was checked against Xray's and grpc-go's source.

gRPC-транспорт теперь понимает форму custom path из Xray для `service_name`. Значение с ведущим `/` — `"/xxx/something/Tun"` — уходит на провод ровно этим путём, как его строит сам Xray; раньше всё значение экранировалось одним сегментом, и сервер отвечал `v2ray-grpc: unexpected status: 404 Not Found`. Значения без ведущего `/` ведут себя байт в байт как раньше. Против живого сервера Xray пока не проверено — путь на проводе сверен с исходниками Xray и grpc-go.

---

<details open>
<summary><h2>🇬🇧 English</h2></summary>

## 🔧 Changed

### 🔗 gRPC `service_name`: a leading `/` means Xray's custom path

- Was: a server configured the Xray way, `serviceName: "/xxx/something/Tun"` (typically behind nginx with the same `location`), could not be reached — `v2ray-grpc: unexpected status: 404 Not Found`. The client escaped the whole value as a single segment and sent `/%2Fxxx%2Fsomething%2FTun/Tun`. ([singbox-launcher#130](https://github.com/Leadaxe/singbox-launcher/issues/130))
- Now: a leading `/` switches to Xray's convention — each segment is escaped on its own, the last segment is the stream name, a `|…` multi-stream tail is dropped. `"/a/b/Tun"` goes on the wire as `/a/b/Tun`, `"/a/b/Stream"` as `/a/b/Stream`.
- Without a leading `/` nothing changed: `"TunService"` → `/TunService/Tun`, `"a/b"` → `/a%2Fb/Tun` (which is also what Xray sends for that form). A guard test pins this byte for byte.
- Applies to the client and to the server (inbound). The lite server still compares the decoded path, so it is more lenient than Xray: a server with `"a/b"` accepts both `/a%2Fb/Tun` and `/a/b/Tun`. The `with_grpc` client still sends the old form unescaped — upstream behaviour, not changed.
- Verification: the `:path` the client sends is captured by a bare HTTP/2 listener and compared with what Xray computes; the convention was checked against the source of Xray (`getServiceName`/`getTunStreamName`, `TunCustomName`) and grpc-go (raw `:path`, split at the last `/`). ⚠️ Not run against a live Xray server yet. ([SPEC 093](https://github.com/Leadaxe/sing-box-lx/blob/lx/SPECS/TASKS/093-GRPC_SERVICE_NAME_CUSTOM_PATH/SPEC.md), [docs §4.1](https://github.com/Leadaxe/sing-box-lx/blob/lx/docs-lx/lx-protocols-transports.md))

- Unchanged: configuration (no new keys, no new validation), the wire format for every existing `service_name` without a leading `/`, all other transports, Clash API, gRPC/lxd and the libbox signatures, the tag sets of desktop, router and AAR builds, the Go toolchain (1.26.8), the four submodules.
- Upstream base is unchanged — sing-box `v1.14.1`. Known drift at the cut: `upstream/stable` is 10 commits ahead, together with a `wireguard-go` bump to v0.0.7. The WireGuard changes require re-basing the `wireguard-go` fork submodule first, so the whole set is still deliberately left for a separate sync.

</details>

<details open>
<summary><h2>🇷🇺 Русский</h2></summary>

## 🔧 Изменено

### 🔗 gRPC `service_name`: ведущий `/` = custom path в конвенции Xray

- Было: сервер, настроенный по-Xray'евски, `serviceName: "/xxx/something/Tun"` (обычно за nginx с таким же `location`), был недостижим — `v2ray-grpc: unexpected status: 404 Not Found`. Клиент экранировал всё значение одним сегментом и слал `/%2Fxxx%2Fsomething%2FTun/Tun`. ([singbox-launcher#130](https://github.com/Leadaxe/singbox-launcher/issues/130))
- Стало: ведущий `/` включает конвенцию Xray — каждый сегмент экранируется отдельно, последний сегмент — имя стрима, хвост `|…` (multi-стрим) отбрасывается. `"/a/b/Tun"` уходит на провод как `/a/b/Tun`, `"/a/b/Stream"` — как `/a/b/Stream`.
- Без ведущего `/` ничего не изменилось: `"TunService"` → `/TunService/Tun`, `"a/b"` → `/a%2Fb/Tun` (так же эту форму шлёт и Xray). Страж-тест держит это байт в байт.
- Работает на клиенте и на сервере (inbound). lite-сервер по-прежнему сравнивает декодированный путь, поэтому он мягче Xray: сервер с `"a/b"` принимает и `/a%2Fb/Tun`, и `/a/b/Tun`. Клиент под `with_grpc` по-прежнему шлёт старую форму без экранирования — апстримное поведение, не меняли.
- Проверка: `:path` клиента снимается голым HTTP/2-листенером и сверяется с тем, что вычисляет Xray; конвенция сверена с исходниками Xray (`getServiceName`/`getTunStreamName`, `TunCustomName`) и grpc-go (`:path` сырой, разрез по последнему `/`). ⚠️ Против живого сервера Xray пока не прогонялось. ([SPEC 093](https://github.com/Leadaxe/sing-box-lx/blob/lx/SPECS/TASKS/093-GRPC_SERVICE_NAME_CUSTOM_PATH/SPEC.md), [дока §4.1](https://github.com/Leadaxe/sing-box-lx/blob/lx/docs-lx/lx-protocols-transports.ru.md))

- Не меняются: конфигурация (новых ключей и новой валидации нет), провод для любого существующего `service_name` без ведущего `/`, остальные транспорты, Clash API, gRPC/lxd и libbox-сигнатуры, наборы тегов desktop-, router- и AAR-сборок, Go-тулчейн (1.26.8), четыре сабмодуля.
- База апстрима без изменений — sing-box `v1.14.1`. Известный дрейф на момент среза: `upstream/stable` впереди на 10 коммитов вместе с бампом `wireguard-go` до v0.0.7. Изменения WireGuard требуют сначала перевести форк-сабмодуль `wireguard-go` на новую базу, поэтому весь набор по-прежнему намеренно оставлен на отдельный синк.

</details>
