Configuration errors now name the element that failed. A rejected config used to report only the position in the list — `initialize outbound[0]: invalid short_id` — which says nothing to a user whose subscription holds a hundred tagged nodes. The message now carries the type and tag as well: `initialize outbound[0] vless[proxy-de-1]: invalid short_id`, in the same form the logger already uses. Applied to all six indexed init loops: DNS servers, endpoints, inbounds, services, outbounds and certificate providers. No new keys, no changed defaults, no wire changes.

Ошибки конфигурации теперь называют элемент, на котором произошёл отказ. Отвергнутый конфиг сообщал только позицию в списке — `initialize outbound[0]: invalid short_id`, — что ничего не говорит пользователю, у которого в подписке сотня узлов с тегами. Теперь в сообщении есть тип и тег: `initialize outbound[0] vless[proxy-de-1]: invalid short_id`, в той же форме, что уже используется в имени логгера. Изменение охватывает все шесть индексированных циклов инициализации: DNS-серверы, endpoint'ы, inbound'ы, сервисы, outbound'ы и провайдеры сертификатов. Новых ключей нет, дефолты и провод не меняются.

---

<details open>
<summary><h2>🇬🇧 English</h2></summary>

## 🔧 Changed

### 🏷️ Config errors now name the element: type and tag, not just the index

- Was: a config rejected at startup reported the element only by its position in the list — `initialize outbound[0]: invalid short_id`. Consuming applications (LxBox, the launcher, `lxd apply`) show the core's text verbatim, and a subscription holds tagged nodes, not numbered ones, so the index did not identify which node to fix.
- Now: the type and tag follow the index, before the colon — `initialize outbound[0] vless[proxy-de-1]: invalid short_id`. The tail repeats the form the logger name already uses (`outbound/vless[proxy-de-1]`), so the error text and the log line read as the same name. An element without an explicit tag gets the index in the tag position: `initialize outbound[0] vless[0]: invalid short_id` — the type alone is still useful.
- Covers all six indexed init loops: DNS server, endpoint, inbound, service, outbound, certificate provider.
- Boundaries: the upstream prefix `initialize <kind>[<i>]` is preserved byte-for-byte, including the wording and case of the kind (`DNS server`, `certificate provider`) — existing parsers matching on it keep working, since the only new text sits between `]` and the colon. Errors produced *inside* the constructors (`invalid short_id`, `unknown udp_relay_mode: …`) are unchanged: the tag is added by the wrapper in `box.New`, not by each protocol. Errors without an index (`initialize router`, `create log factory`) are untouched. Logger name format, Clash API, gRPC/lxd and the libbox signatures do not change. ([SPEC 092](https://github.com/Leadaxe/sing-box-lx/blob/lx/SPECS/TASKS/092-INIT_ERROR_NAMES_TAG/SPEC.md))

- Unchanged: configuration (no new keys), the wire format of every protocol, the defaults, the logger name format, Clash API, gRPC/lxd and the libbox signatures, the tag sets of desktop, router and AAR builds, the Go toolchain (1.26.8), the four submodules.
- Upstream base is unchanged — sing-box `v1.14.1`. Known drift at the cut: `upstream/stable` is 10 commits ahead, together with a `wireguard-go` bump to v0.0.7. The WireGuard changes require re-basing the `wireguard-go` fork submodule first, so the whole set is still deliberately left for a separate sync.

</details>

<details open>
<summary><h2>🇷🇺 Русский</h2></summary>

## 🔧 Изменено

### 🏷️ Ошибки конфигурации называют элемент: тип и тег, а не только индекс

- Было: конфиг, отвергнутый при запуске, называл элемент только позицией в списке — `initialize outbound[0]: invalid short_id`. Приложения-потребители (LxBox, лаунчер, `lxd apply`) показывают текст ядра дословно, а в подписке узлы с тегами, а не с номерами, — по индексу пользователь не находил, какой именно узел чинить.
- Стало: за индексом, перед двоеточием, идут тип и тег — `initialize outbound[0] vless[proxy-de-1]: invalid short_id`. Хвост повторяет форму, которую уже использует имя логгера (`outbound/vless[proxy-de-1]`), поэтому текст ошибки и строка лога читаются как одно и то же имя. У элемента без явного тега в позиции тега стоит индекс: `initialize outbound[0] vless[0]: invalid short_id` — тип всё равно полезен.
- Изменение охватывает все шесть индексированных циклов инициализации: DNS server, endpoint, inbound, service, outbound, certificate provider.
- Границы: апстримный префикс `initialize <kind>[<i>]` сохранён дословно, включая формулировку и регистр вида (`DNS server`, `certificate provider`), — существующие парсеры, матчащие на него, продолжают работать, потому что новый текст стоит между `]` и двоеточием. Ошибки, возникающие *внутри* конструкторов (`invalid short_id`, `unknown udp_relay_mode: …`), не менялись: тег добавляет обёртка в `box.New`, а не каждый протокол. Ошибки без индекса (`initialize router`, `create log factory`) не тронуты. Формат имени логгера, Clash API, gRPC/lxd и libbox-сигнатуры не меняются. ([SPEC 092](https://github.com/Leadaxe/sing-box-lx/blob/lx/SPECS/TASKS/092-INIT_ERROR_NAMES_TAG/SPEC.md))

- Не меняются: конфигурация (новых ключей нет), провод любых протоколов, дефолты, формат имени логгера, Clash API, gRPC/lxd и libbox-сигнатуры, наборы тегов desktop-, router- и AAR-сборок, Go-тулчейн (1.26.8), четыре сабмодуля.
- База апстрима без изменений — sing-box `v1.14.1`. Известный дрейф на момент среза: `upstream/stable` впереди на 10 коммитов вместе с бампом `wireguard-go` до v0.0.7. Изменения WireGuard требуют сначала перевести форк-сабмодуль `wireguard-go` на новую базу, поэтому весь набор по-прежнему намеренно оставлен на отдельный синк.

</details>
