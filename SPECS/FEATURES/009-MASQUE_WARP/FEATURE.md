# FEATURE 009 — MASQUE_WARP — outbound MASQUE (CONNECT-IP)

| Поле | Значение |
|------|----------|
| Тип | Продуктовая фича |
| Build-tag | — собственного нет, но нужны апстрим-теги `with_quic` (+`with_gvisor`): без них тип `masque` в конфиге отвергается с ошибкой «QUIC не собран» |
| Состояние | ✅ Реализовано |

## Назначение

Outbound типа `masque` — туннель по протоколу MASQUE в варианте **CONNECT-IP**
([RFC 9484](https://www.rfc-editor.org/rfc/rfc9484)), переносящий IP-пакеты
внутри HTTP-соединения. Основной сценарий — Cloudflare WARP.

Вариант с IP-пакетами выбран сознательно: WARP работает именно так, вариант
с переносом отдельных UDP-датаграмм для этой задачи непригоден.

⚠️ Не путать с полями `id`/`ip`/`ib` в [AWG](../003-AWG/FEATURE.md) —
там «masquerade» означает совсем другой механизм.

## Контролируемые параметры

Внутри блока outbound'а типа `masque`.

| Ключ | Тип | Дефолт | Смысл |
|------|-----|--------|-------|
| `profile` | `cloudflare` · `standard` | `cloudflare` | Набор умолчаний и поведения: WARP либо обычный сервер по RFC |
| `vhttp` | `h3` · `h2` · `auto` | `auto` | **Версия HTTP**, несущая туннель: `auto` — h3 с автопадением на h2 (SPEC 074 v2; на `standard` тихо = h3), `h3` — поверх QUIC (UDP), `h2` — поверх TCP |
| `network_list` | `tcp` · `udp` | оба | **Протоколы L4**, которые пускаются в туннель |
| `server`, `server_port` | адрес | — | Точка подключения |
| `private_key`, `public_key` | base64 | — | Ключи; обязательны для профиля `cloudflare` |
| `ip` | адрес с маской | — | Локальный адрес туннеля (IPv4); без маски трактуется как одиночный |
| `ipv6` | адрес с маской | — | Локальный адрес туннеля (IPv6) |
| `uri` | строка | по профилю | Адрес запроса на установку туннеля |
| `tls` | объект | — | **Стандартный блок TLS** outbound'а: имя сервера, отключение проверки, отказ от SNI, фрагментация ClientHello и прочее — тот же контейнер, что у любого TLS-outbound |
| `mtu` | целое | 1280 | MTU внутреннего стека |
| `idle_timeout` | длительность | 5m | Простой, после которого туннель усыпляется; отрицательное — не усыплять |
| `keep_alive_period` | длительность | 30s | Период поддержания соединения (для `h3`); отрицательное — выключить |

Требуется хотя бы один из `ip` / `ipv6`.

**Имя сервера по умолчанию — нейтральный популярный хост, а не имя эндпоинта.**
Назвать MASQUE-эндпоинт в ClientHello — ровно то, по чему его отсекает DPI;
подлинность эндпоинта проверяется пиннингом ключа, а не именем, поэтому имя
свободно отличается. Отдельно поддержан случай «SNI не слать вовсе»: часть
эндпоинтов отдаёт настоящий сертификат только на приветствие без имени.

⚠️ **Совместимость: старая форма конфига живёт до `v1.14.0-lx.30`.** Раньше
версия HTTP задавалась ключом `network` (который на любом другом outbound
означает список `tcp`/`udp` — то есть обратное), а настройки TLS были плоскими
ключами рядом с остальными. Обе формы принимаются; использование старой даёт
одно предупреждение на outbound, а расхождение старой и новой формы —
**ошибка**, а не молчаливый выбор одной из них. Плоские флаги-переключатели
переносятся только во включённом положении: выключенное неотличимо от
незаданного. Таблица соответствия — в конфиг-справочнике фичи.

⚠️ **Фрагментация UDP разрешена по умолчанию** — в отличие от обычных
outbound'ов, где исходящая датаграмма помечается «не фрагментировать».
Туннель по своей природе добавляет к каждому пакету заголовки инкапсуляции,
и при вложении (masque внутри другого туннеля) внешняя датаграмма штатно
перерастает MTU: с запретом фрагментации она молча пропадала бы вместо
разбиения. Явный `udp_fragment: false` возвращает запрет —
см. [HOTFIXES](../004-HOTFIXES/FEATURE.md), запись 028.

## Входы

- **Конфиг** (выше) и **ключи**, полученные при регистрации на стороне
  клиентского приложения.
- **Трафик**, маршрутизируемый в этот outbound.

## Выходы

- **HTTP-соединение к серверу** (поверх QUIC либо TCP), несущее
  IP-пакеты туннеля.
- **Расшифрованный трафик** обратно в маршрутизацию.

## Data flow

```
конфиг + ключи → установка HTTP-соединения (h3 или h2)
               → запрос CONNECT-IP → туннель установлен
                          │
трафик приложения → внутренний IP-стек → IP-пакеты → в туннель
                          │
        простой дольше idle_timeout → туннель усыпляется
        следующее обращение        → туннель восстанавливается
```

## Правила и гарантии

- **Ключи не добываются ядром.** Регистрация выполняется приложением,
  ядро получает готовые ключи из конфига.
- **Туннель усыпляется при простое** и восстанавливается по обращению —
  ресурсы неактивного туннеля не удерживаются.
- **Отмена запроса на установку соединения работает**: попытка подключения
  прекращается вместе с отменой, а не висит до собственного тайм-аута.
- **Значение вида `tcp` в `vhttp` — ошибка**, а не молчаливая подмена смысла.
- **Путь `h2` пользуется общим TLS-слоем ядра** наравне с остальными
  TLS-outbound'ами — в частности, получает фрагментацию приветствия, включая
  автоматическую при диале через промежуточный узел (см. [HOTFIXES](../004-HOTFIXES/FEATURE.md),
  запись 060). На `h3` это не распространяется: QUIC не несёт TLS поверх TCP.

## Границы

- **`auto` = h3, затем h2.** Ждёт QUIC-хендшейк ограниченное время (3 с) и при
  неуспехе поднимает туннель по h2; победивший режим запоминается на процесс, так
  что потолок оплачивается один раз. Отмена вызывающим fallback не запускает.
  Нужен там, где эндпоинт **молча игнорирует QUIC** с конкретного выходного IP —
  отказа нет, есть тишина (SPEC 074).
- **`h3` не проходит через TCP-only-хоп** (HTTP-прокси в `detour`, звено
  VLESS/Trojan в цепочке): такой хоп переносит только TCP, а `h3` работает
  поверх UDP, причём отказа на проводе нет — только тишина. Дефолтный `auto`
  спасается сам (падает на h2, профиль `cloudflare`, MTU ≤ 16000); явный пин
  `vhttp: "h3"` в такой схеме — зависание каждого dial'а до дедлайна.
- **Подлинность эндпоинта проверяется сверкой ключа, а не цепочкой
  сертификатов** — это остаётся особенностью фичи и живёт поверх общего
  TLS-блока, который отвечает за всё остальное.
- **Поведение профиля `cloudflare` определяется реальным сервисом** —
  синтетический стенд его не воспроизводит.

## Задачи фичи

| Задача | Роль | Статус |
|--------|------|--------|
| [021 — MASQUE_CONNECT_IP_OUTBOUND](../../TASKS/021-MASQUE_CONNECT_IP_OUTBOUND/SPEC.md) | Outbound целиком: профили, транспорты, туннель | C |
| [062 — MASQUE_CONFIG_SCHEMA_MIGRATION](../../TASKS/062-MASQUE_CONFIG_SCHEMA_MIGRATION/SPEC.md) | Конфиг к общему стандарту: `vhttp` вместо `network`, штатный блок TLS вместо плоских ключей, старая форма на алиасах с предупреждением | C |

Полная таблица параметров — `CONFIG.md` в задаче 021; таблица соответствия
старых и новых ключей — в конфиг-справочнике ([docs-lx/lx-config.ru.md §4](../../../docs-lx/lx-config.ru.md)).

Смежное: [028](../../TASKS/028-NESTED_TUNNEL_UDP_FRAGMENT/SPEC.md) — туннель
не работал внутри другого туннеля, см. [HOTFIXES](../004-HOTFIXES/FEATURE.md).

## Особенности сопровождения

- **Проверять на реальном WARP.** Профиль завязан на поведение конкретного
  сервиса, а не на текст RFC.
- **Отмена операций — не мелочь.** Игнорирование отмены на этапе установки
  соединения давало зависание навсегда; это была самая дорогая находка
  аудита ([AUDITS](../010-AUDITS/FEATURE.md)).
- **Вложенность требует внимания к размеру пакетов** — см.
  [HOTFIXES](../004-HOTFIXES/FEATURE.md).
