# SPECS — sing-box-lx (Spec Kit)

Два уровня документации:

| Каталог | Уровень | Содержимое |
|---------|---------|------------|
| **[FEATURES/](FEATURES/README.md)** | Крупные блоки | Фича целиком — как она устроена сейчас, поверх всех задач, которые её строили |
| **[TASKS/](TASKS/)** | Единицы работы | Папки `NNN-NAME` — конкретная задача: фича, баг или исследование |

Задача — единица работы (её делают и закрывают). Фича — то, что живёт
в продукте и складывается из нескольких задач.

Фичи двух родов: **продуктовые** (AWG, XHTTP, балансировка, наблюдаемость,
MASQUE, энергосбережение, VLESS-шифрование, DNS-группа) и **процессные** — постоянная
работа форка поверх чужой базы: чинить за апстримом (`HOTFIXES`),
синхронизироваться с ним (`UPSTREAM_SYNC`), собирать и выпускать
(`BUILD_CI_CD`), проверять себя (`AUDITS`), исследовать (`RESEARCH`).

**Ссылаться лучше на фичу, а не на задачу** — фича переживает переномерацию
и рефакторинг. Ссылка на задачу уместна, когда нужен конкретный разбор
внутри фичи. Каждая задача несёт обратную ссылку `**Фича:**` под заголовком.

## Задачи: `TASKS/NNN-NAME`

Внутри: SPEC.md → PLAN.md → TASKS.md → IMPLEMENTATION_REPORT.md.

| Часть | Значение | Расшифровка |
|-------|----------|-------------|
| **NNN** | 001, 002, … | Сквозной номер — **стабильный якорь**, не меняется никогда |
| **NAME** | UPPER_SNAKE | Название |

Имя папки **не несёт** тип/статус: они меняются по ходу задачи, а имя должно
оставаться стабильным (на него ссылаются из кода, доков и сабмодуля). Ссылки
давай короткой формой `SPECS/TASKS/NNN-NAME` — она переживёт смену статуса.

## Тип и статус — в шапке SPEC.md + Roadmap

Источник правды по типу/статусу — **таблица-шапка в начале `SPEC.md`** каждой
задачи; [Roadmap](#roadmap-план-задач) ниже её агрегирует.

```markdown
| Поле | Значение |
|------|----------|
| Тип | F (feature) \| B (bug) \| Q (question/исследование) \| R (refactor) |
| Статус | N (new) \| O (open) \| W (wait) \| I (implemented) \| C (complete) \| D (done, device-verified) |
```

Разница между тремя «готовыми»: **I** — код, тесты и сборка в дереве, но полевой
проверки ещё не было; **C** — задача закрыта по своим критериям приёмки;
**D** — сверх того подтверждена прогоном на живом устройстве. Задача может
уйти в релиз в статусе `I`; `D` ставится только по факту полевого прогона.

## Файлы внутри папки

| Файл | Назначение |
|------|------------|
| **SPEC.md** | Что и зачем — проблема, требования, критерии приёмки |
| **PLAN.md** | Как строить — архитектура, изменяемые файлы, зона касания upstream |
| **TASKS.md** | Чеклист по этапам |
| **IMPLEMENTATION_REPORT.md** | Отчёт после реализации |
| **HISTORY.md** | Хронология: как делали раньше, почему переделали. Только при смене архитектуры фичи (см. правило структуры ниже) |

### Структура SPEC.md — актуальное состояние сверху, НЕ хронология

**Правило:** SPEC.md описывает ТЕКУЩУЮ (актуальную) архитектуру фичи в первую очередь. Порядок разделов — от актуального состояния к деталям, НЕ по хронологии разработки.

- Верх SPEC.md = как фича устроена СЕЙЧАС (актуальная архитектура), затем детали/контракт/критерии.
- НЕ вести SPEC.md как дневник («сначала сделали так, потом в rc.9 исправили…»). Пометки вида «ИСПРАВЛЕНО в rc.N», «прежнее утверждение неверно», отвергнутые подходы — НЕ в SPEC.md.
- Когда архитектура фичи МЕНЯЕТСЯ (переделали механизм): SPEC.md переписывается под новое состояние, а старое состояние + обоснование смены (как делали, почему это оказалось неверно, почему выбрали новое) выносятся в **HISTORY.md** этой же папки.
- Читатель SPEC.md должен понять, как всё работает СЕЙЧАС, не продираясь через историю решений. История — по запросу, в HISTORY.md.

## Конфигурация фич

Пользовательский конфиг XHTTP и AmneziaWG 2.0 (поля + примеры) — **[../docs-lx/lx-config.md](../docs-lx/lx-config.md)**.

## Корень SPECS

| Файл | Назначение |
|------|------------|
| **CONSTITUTION.md** | Принципы, приоритеты реализации, правила; ориентиры и референсы — в этом файле выше |
| **IMPLEMENTATION_PROMPT.md** | DoD, git/ребейз-ритуал, контракт выхода |

## Ориентиры и референсы

Заметки для реализации, вынесенные из [CONSTITUTION.md](CONSTITUTION.md) (там — только принципы
и правила). Факты апстрим-линии 1.14, проверять на каждом мерже:

- **v2ray-транспорты** диспатчатся `switch` по `options.Type` в `transport/v2ray/transport.go`
  (`NewClientTransport`/`NewServerTransport`); константы — `constant/v2ray.go`, опции —
  `option/v2ray_transport.go`. VLESS/VMess/Trojan ходят через общий транспорт — пер-протокольных
  правок не требуется.
- **WireGuard — endpoint**: `protocol/wireguard/endpoint.go`, регистрация
  `endpoint.Register[option.WireGuardEndpointOptions](registry, C.TypeWireGuard, NewEndpoint)`,
  проводка в `include/wireguard.go` (+ `wireguard_stub.go`). Девайс — `transport/wireguard`;
  зависимость `github.com/sagernet/wireguard-go` заменена `replace` на форк-сабмодуль.
- **libbox command-протокол** — gRPC-сервис `StartedService` в `daemon/started_service.proto`
  (+ регенерируемые `*.pb.go`/`*_grpc.pb.go`), клиент `experimental/libbox/command_client.go`.
  Опциональные RPC гейтятся build-tag'ом по парному паттерну
  `daemon/started_service_usbip{,_stub}.go` — образец для конституции §3.6.
- **TLS-клиенты** — общий слой `common/tls`: STD, uTLS и REALITY строятся через
  `NewClientWithOptions`; всё, что должно действовать на все три (фрагментация, дефолты под
  `detour`), ставится там, а не в отдельном движке (уроки SPEC 060/088).

Референсы — только как образец, код «как есть» не тянуть:

- **AWG** — [`hoaxisr/amnezia-box`](https://github.com/hoaxisr/amnezia-box) (submodule +
  `patches/amneziawg-go`) — исторический образец; фактическая схема своя: форк-сабмодуль
  `submodules/wireguard-go` = [Leadaxe/wireguard-go-awg2-lx](https://github.com/Leadaxe/wireguard-go-awg2-lx).
- **Спецификации протоколов** — Xray-core (XHTTP: `mode`/`path`/`host`/`extra`; REALITY: сервер
  в XTLS/REALITY; VLESS `encryption`), amneziawg-go / amneziawg-tools (AWG 2.0/3.x), Cloudflare
  WARP (MASQUE CONNECT-IP). Где смотреть — [docs-lx/lx-reference-cores.md](../docs-lx/lx-reference-cores.md).
- **Clash API как функциональный эталон** для расширений §3.6 — `experimental/clashapi/`
  (`proxies.go` per-node delay, `rules.go` таблица правил): что именно пробрасываем в
  CommandClient. Код не тянуть — повторяем семантику через нативный канал.

## Методология: фича → задачи → реализация

**Порядок работы — сверху вниз. Сначала фича, потом задачи.**

Фича не пишется постфактум как сводка сделанного. Она пишется **первой** —
до задач и до кода — и фиксирует полный скоуп: что система должна делать,
чем она управляется, что принимает и что отдаёт. Только затем скоуп режется
на задачи, и только затем пишется код.

### 1. Фича — полный скоуп (до задач)

`FEATURES/NNN-NAME/FEATURE.md` описывает систему **как чёрный ящик**:

| Раздел | Содержание |
|--------|------------|
| **Назначение** | Что фича даёт пользователю и зачем существует |
| **Контролируемые параметры** | Все ручки управления: конфиг-ключи (тип, значения, дефолт), build-теги, переменные окружения |
| **Входы** | Что ящик принимает: конфиг, трафик, внешние события |
| **Выходы** | Что отдаёт: провод, RPC/UI, наблюдаемое поведение |
| **Data flow** | Путь данных через ящик — от входа до выхода, в терминах стадий, не файлов |
| **Правила и гарантии** | Инварианты, взаимоисключения, fail-fast, что валидируется и когда |
| **Границы** | Что фича намеренно **не** делает; ограничения платформ |

### 2. Разбиение на задачи

Скоуп фичи режется на задачи `TASKS/NNN-NAME` — каждая берёт свой кусок
и ссылается на фичу строкой `**Фича:**` под заголовком. Фича при этом
получает задачу в свою таблицу «Задачи фичи».

### 3. Реализация

По задаче: SPEC.md → PLAN.md → TASKS.md → код, с учётом
IMPLEMENTATION_PROMPT и CONSTITUTION. Заканчивается IMPLEMENTATION_REPORT.md,
DoD-чеклистом и статусом `C`.

### 4. Поддержание фичи

`FEATURE.md` — **живой дизайн системы**, а не архив. Меняется поведение,
параметр или гарантия → правится фича, а не только задача. Действует то же
правило, что для SPEC.md: описывается **текущее** состояние, без хронологии.

### ⚠️ Главное правило: фича отвечает ЧТО, а не КАК

В `FEATURE.md` **не место** именам файлов, функций, полей структур, пакетов
и слоёв кода. Реализация — в задачах (`PLAN.md`), фича описывает наблюдаемое
поведение и контракт.

Критерий проверки: **если реализацию завтра переписать с нуля, FEATURE.md
не должен измениться — пока не изменилось поведение.**

| ❌ КАК (реализация) | ✅ ЧТО (контракт) |
|---|---|
| «Sticky-ключ читает `metadata.Domain`, а не `destination.Fqdn`, потому что роутер перезаписывает `metadata.Destination`» | «Закрепление считается от домена исходного запроса, а не от резолвленного IP — смена IP узла закрепление не срывает» |
| «Слой `transport/wireguard/masque_awg.go` генерирует `i1` из полей `option.AmneziaWGOptions`» | «`id`/`ip`/`ib` и явный `i1` взаимоисключаются: конфиг с обоими отвергается до старта туннеля» |
| «Тик роутера обходит `Endpoints()`, а не `Outbounds()`, тянет `EndpointManager` из ctx» | «Засыпанию подлежат все WG/AWG-узлы профиля независимо от способа объявления» |

Что **остаётся** в фиче, несмотря на близость к коду: **конфиг-ключи**
(это пользовательский контракт), **build-теги** (внешняя ручка сборки),
**имена RPC и полей протокола** (контракт с клиентом).

Процессные фичи (`HOTFIXES`, `UPSTREAM_SYNC`, `BUILD_CI_CD`, `AUDITS`,
`RESEARCH`) под шаблон чёрного ящика не подпадают — у них нет конфига
и провода. Их форма — реестры и watchlist, см. каждую.

## Roadmap (план задач)

| # | Задача | Статус | Суть |
|---|--------|--------|------|
| **001** | FORK_BOOTSTRAP | **C** | Remotes, ветка `lx`, `Makefile.lx`, версия `-lx` (ldflags), CI-скелет, `lx-test/config` — ✅ собрано/проверено |
| **002** | XHTTP_CLIENT_TRANSPORT | **C** | ✅ **live-validated** против Xray (3x-ui): packet-up/auto работают (handshake+DNS+HTTPS+download); stream-one — был баг, **исправлен в 011** |
| **003** | AWG2_CLIENT_ENDPOINT | **C** | ✅ **Функционален, проверен живым AWG2-сервером** (handshake+keepalive+трафик). merged-форк Leadaxe/wireguard-go (sagernet+обфускация) через submodule; S1–S4/H1–H4/I1–I5 |
| **004** | BUILD_CI_RELEASE | **C** | ✅ `Makefile.lx`/libbox-теги, дешёвый CI (lint+build-check на push; cross×6+AAR на dispatch), `lx-release.yml` (**релиз v1.13.13-lx.3 опубликован** — 6 desktop + 2 AAR), поставка libcronet: dll в windows-архивах (purego), darwin — CGO-статика на macos-раннере (naive работает), `lx-rebase.yml` (авто-ребейз → PR/issue, демо зелёное) |
| **005** | AWG2_RANGED_MAGIC_HEADERS | **C** | ✅ **Проверено живым awg2-сервером с ranged-конфигом** (handshake+трафик). Диапазонные `H1`–`H4` (`"N-M"`) из awg2-экспортов: `option.MagicHeader` (number\|string) → spec-строка в IpcSet; vendored wireguard-go уже умел |
| **006** | LINUX_MUSL_STATIC_ROUTER_BUILDS | **C** | ✅ **CI-приёмка 4/4 арки статикой** (amd64/arm64/armv7/mipsle-softfloat, `statically linked`, libdl=0, naive сохранён). musl-сборки под роутеры по подобию upstream build.yml (cronet-go + Chromium musl-toolchain, `with_musl`). Чинит [#1](https://github.com/Leadaxe/sing-box-lx/issues/1) (`libdl.so.2` на AsusWRT + armv7). CI-only, без Go-кода |
| **007** | AWG_OVER_WIREGUARD_DETOUR_GUARD | **C** | ⛔️ **Guard СНЯТ (lx.11, 2026-07-18).** Первопричина ушла на новом графте (re-graft AWG на wireguard-go v0.0.5 + SPEC 025 padding + SPEC 026 reserved-clear): AWG-over-WireGuard больше не вешает ядро, поднимается и несёт трафик (e2e mac-стенд: AWG s4=12/ranged h1..h4 через плоский WG, handshake+keepalive+HTTP ок). Удалены оба guard'а (Start-guard + selector-guard), adapter-хуки `ConsumersOf`/`AmneziaWGSuspendable` и все guard-тесты; SPEC 020 idle-suspend сохранён. Осталось: Android field-тест (старый hang был Android-only). App-side §130-гейт LxBox снят синхронно. Исторический контекст — в [SPEC.md](TASKS/007-AWG_OVER_WIREGUARD_DETOUR_GUARD/SPEC.md). Was [#2](https://github.com/Leadaxe/sing-box-lx/issues/2) |
| **008** | AWG_JUNK_PARAM_VALIDATION | **C** | ✅ **Код+тесты+DoD**. Bug в 003 (найден при 007): `jmin > jmax` паникует `rand.Int` в amneziawg-go (краш в timer-горутине). `validateJunk` в `awgIpcLines` отвергает на уровне конфига (`check`/старт), без паники. Узко — только краш-кейс; jc-несогласованность осознанно не трогаем (минимальный дифф, совместимость). Чинит [#3](https://github.com/Leadaxe/sing-box-lx/issues/3) |
| **009** | WIRESOCK_MASQUERADE_PROFILES | **C** | ✅ **Код+тесты+DoD; механизм проверен вживую (туннель + трафик на 009), релиз `v1.13.13-lx.11`.** WireSock-стиль `id`/`ip`/`ib` (домен/протокол/браузер) — декларативный сахар над `I1` CPS. Профили **quic** (1-RTT short header) / **dns** (EDNS OPT response) / **stun** (Binding Success Response) / **sip** (200 OK response), структуры портированы из open-source WireSock `amneziawg-proxy/src/transform.rs` (MIT). Механизм — **I1 only** (S1–S4 невозможен против WARP, сабмодуль не трогаем). `id` обязателен только для dns/sip (там идёт на провод), для quic/stun опционален. Строгая LDH-валидация домена (security-граница: инъекция в SIP/DNS). `ib` — без JA3-fingerprint (честно задокументировано). Все профили приняты реальным `newObfChain`; `sing-box check` зелёный; адверсариальный ревью (6 агентов) — 0 находок |
| **010** | WG_ENDPOINT_GRO_SPLIT_BRAIN | **C** | ✅ **Корень подтверждён на железе, фикс верифицирован** (download 0.44→20.7 Mbps), вмержен в `lx` (lx.14). Bug: WG-**endpoint** без `detour` на Android режет download (GRO split-brain — UDP_GRO включён, а receive-путь linux-only). Фикс — гейт `UDP_GRO` за `!android` в сабмодуле `wireguard-go` (`conn/`). UDP/WG-only |
| **011** | XHTTP_STREAM_ONE_DOWNLINK | **C** | ⚠️ **Принято на синтетике; лайв НЕ прогонялся** (нет reality+xhttp ноды). Bug в 002 (жалоба): `vless+reality+xhttp+mode:auto` не работал. Корень (сверено с Xray + [issue #5635](https://github.com/XTLS/Xray-core/issues/5635) + hiddify): stream-one слал `<path>/<sessionId>`, а Xray-сервер роутит stream-one только при пустом sessionId → downlink не-VLESS → `unknown version`. Фикс: голый путь без sessionId; `mode:auto`+reality → stream-one (детект reality по имени типа, без with_utls-зависимости). Юнит-тесты (URL-layout + reality-детект), `check`, сборки зелёные. Ветка `lx-xhttp-streamone`, **в `lx` не влито**. ⚠️ Лайв состоялся 2026-08-01 и вскрыл, что фикс был НЕПОЛНЫМ: вместе с sessionId из пути был срезан и завершающий слэш, из-за чего сервер 404-ил запрос — корень найден в [043](TASKS/043-XHTTP_STREAM_ONE_PATH_PREFIX/SPEC.md) (попутно [042](TASKS/042-XHTTP_STREAM_GRPC_CONTENT_TYPE/SPEC.md) — gRPC-тип контента) |
| **012** | TCP_DOWNLINK_STALL_ZOMBIE_CONNS | **C** | ⚠️ **НЕ воспроизводится на lx.14** (статус закрытия — «not reproducible»). Симптом (↑517 ↓0, WhatsApp/Telegram «висят») наблюдался на РАЗНЫХ нодах, включая WG → зонтик над «↓0»-сталлом, не один баг. WG-долю закрыл [010](TASKS/010-WG_ENDPOINT_GRO_SPLIT_BRAIN/SPEC.md) (UDP/WG-only). Для не-WG (VLESS/reality) отдельного код-фикса нет — симптом сейчас не воспроизводится без подтверждённого объяснения. Артефакты: зонд `LX_CONN_TRACE` (не прогнан в бою), [PROBE.md](TASKS/012-TCP_DOWNLINK_STALL_ZOMBIE_CONNS/PROBE.md) |
| **013** | PACKAGE_NAME_REGEX_RULE_ITEM | **C** | ✅ **Код+тесты, сборки/vet/gofmt зелёные.** Бэкпорт апстрим-фичи 1.14 ([941ce58b](https://github.com/SagerNet/sing-box/commit/941ce58b)) на базу 1.13.13 **без** полной миграции: rule-item `package_name_regex` (regex-матчинг Android-пакета) для route/DNS/headless. Новый `route/rule/rule_item_package_name_regex.go` + поля/регистрация/cond в 6 файлах роутинга. Хунк `RuleSetVersion5` из коммита НЕ переносился (это rule-set v5, не фича). Полная миграция на 1.14 отложена до **v1.14.0 stable** (feasibility: ~1,5–2 дня, риск — ребейз AWG-подмодуля; `lx-rebase.yml` сам исключает alpha) |
| **014–021** | *(см. шапки в `SPECS/TASKS/NNN-*/SPEC.md`)* | **C** | Command-протокол RPC (014/015), connections-мьютекс (016), Connection.Detour (017), DNS-query-стрим (018), URLTest пул/sticky (019), multi-WG idle-suspend (020), MASQUE CONNECT-IP outbound (021). Источник статуса — шапка каждого SPEC.md |
| **022** | LX_DEEP_AUDIT | **C** | 🔍 **Аудит-исследование + ремедиация.** Многоагентный аудит всей LX-дельты по 10 осям + адверсариальная верификация каждой находки. 32→**27 подтверждено** (0 critical, 1 high, 1 medium, 6 low, 19 nit), 5 опровергнуто. **Исправлено 24/27** (ветка `lx-spec022-audit-fixes`): P0 [#1](TASKS/022-LX_DEEP_AUDIT/SPEC.md) masque h2 CONNECT висел навечно (ctx игнорировался); P1 #2 idle-suspend воскрешал guard-suspended AWG (hang ядра Android); + все P2/P3. Осознанно пропущены #12/#17/#18 (санкционированные компромиссы / ребейз-цена). Реестр + ремедиация в [SPEC.md](TASKS/022-LX_DEEP_AUDIT/SPEC.md) |
| **023** | MUSL_TOOLCHAIN_MIRROR | **C** | 🛠 **Durable-зеркало Chromium musl-тулчейна.** `snapshot.debian.org` периодически 503-ит и блокирует релиз (`v1.14.0-lx.2-rc.1` падал дважды на нём; `actions/cache` промахивается из-за ref-scoping тегов). Producer-workflow (`lx-musl-toolchain-mirror.yml`) заливает собранный тулчейн 4 арок в release-ассет `musl-toolchain-cache`; `lx-release.yml` восстанавливает его на cache-miss ДО фолбэка на snapshot. Источники: actions/cache → lx-mirror → snapshot. Обе workflow lx-owned, upstream-дифф нулевой. Зеркало заполнено, restore проверен |
| **024** | RUNTIME_LOOP_GUARD | **DEFERRED** | 🔁 **Guard от runtime-колец в detour/selector.** Статическое кольцо ядро отклоняет на старте (`circular outbound dependency`); собранное в рантайме через `SelectOutbound` — **нет** защиты → `fatal stack overflow`, падение процесса. Проработана событийная модель E1–E5 + `topologyMu`-линеаризация гонки; адверсарская проверка (7 агентов): deadlock/false-positives **HOLDS**, но TOCTOU **BREAKS** — даже полный ядерный guard негерметичен без покрытия endpoint-mgr/Remove/history-side-channels и без устранения pointer↔tag расхождения после рантайм-`Create`. **Решение (2026-07-06): защита на уровне UI (LxBox), ядро не трогаем.** Design record: [SPEC.md](TASKS/024-RUNTIME_LOOP_GUARD/SPEC.md) |
| **025** | AWG_TRANSPORT_PADDING_OVERRUN | **C** | 💥 **Класс config-value крашей AWG-графта, device-verified.** Основной: transport padding (`s4>0`) писал за границу исходящего буфера → `SIGABRT` (`index out of range [123] length 76`) в `RoutineSequentialSender` на первом пакете данных (буфер `InputPacket`/`InputPackets` без запаса под сдвиг). Разбор вскрыл ещё 4 того же вида: rx-байты считались ×2 (дубль блока в `receive.go`), `jmax<jmin`/`i1`–`i5`-длины/полнодиапазонный magic-header роняли handshake. Все — в `submodules/wireguard-go` (форк, `with_awg`). Красно-зелёный `transport_padding_test.go` + `obf_guards_test.go`. Базовый WG (`s4=0`) не затронут. Второй слой к fail-fast [008](TASKS/008-AWG_JUNK_PARAM_VALIDATION/SPEC.md). Релиз `v1.14.0-lx.8-rc.1`. [SPEC.md](TASKS/025-AWG_TRANSPORT_PADDING_OVERRUN/SPEC.md) |
| **026** | AWG_MAGIC_VS_RESERVED_CLEAR | **C** | 🎭 **Reserved-clear уничтожал AWG magic.** Байты 1-3 (Cloudflare WARP reserved) обнулялись БЕЗУСЛОВНО на каждом принятом пакете во всех bind'ах. AWG читает magic как `Uint32(packet[padding:])` — при малом `s1/s2/s4` (0-3) magic пересекает байты 1-3 → обнуление рушит его → пакет (вкл. handshake) дропается → нода не встаёт. Затрагивало ОБА пути (StdNetBind без detour + ClientBind с detour). Обычный WG и AWG с padding≥4 (issue #8: s1=15) не задеты. Фикс: гейт `hasReserved()` на всех 5 receive-clear (linux/darwin/windows/detour) — обнулять только при заданном WARP-reserved. e2e red/green (padding=0). [SPEC.md](TASKS/026-AWG_MAGIC_VS_RESERVED_CLEAR/SPEC.md) |
| **027** | UTLS_OVER_QUIC | **C** | 🔬 **Исследование без реализации: почему uTLS-фингерпринт (`tls.utls`, `fp=chrome`) недостижим поверх QUIC-протоколов (`hysteria2`/`tuic`).** Конфиг проходит `NewOutbound` (нода выглядит живой), но каждый QUIC-хендшейк падает `unsupported usage for uTLS` — построчный причинно-следственный разбор блокера по трём зависимостям (`metacubex/utls v1.8.4`, `sagernet/quic-go`, `sing-quic`), adversarial-верификация (10 агентов, 0 ошибок). Реализация отложена — настоящий фикс требует поддержки в апстрим-зависимостях. Массовый источник симптома — публичные Xray-подписки, вешающие `fp=chrome` на все ноды подряд. [SPEC.md](TASKS/027-UTLS_OVER_QUIC/SPEC.md) |
| **028** | NESTED_TUNNEL_UDP_FRAGMENT | **C** | 🧩 **Вложенные туннели через `detour` не ходили — нижнее плечо форсило DF.** Реальный (нижний) UDP-сокет `wireguard`/`masque` открывается через `common/dialer`, а тот по умолчанию ставит DF (`IP_PMTUDISC_DO`/`IP_DONTFRAG`). Для вложения (masque/wg/awg в `detour` в любых комбинациях) внешняя датаграмма штатно великовата (инкапсуляция +32 WG +`s4` AWG на каждый пакет) → с DF молча дропается (`sendmsg: message too long`) вместо фрагментации → внутренний туннель не встаёт или не несёт данные. Direct-ноды через тот же detour работали (мелкие датаграммы). Фикс: endpoint+masque ставят `UDPFragmentDefault=true` (opt-out как у direct/hysteria2/tuic); явный `"udp_fragment": false` возвращает DF. Юнит DF-флага (оба bind-пути) + e2e AWG-over-AWG через detour (fits+фрагментация). Остаток: field CPH2411. [SPEC.md](TASKS/028-NESTED_TUNNEL_UDP_FRAGMENT/SPEC.md) |
| **029** | ENDPOINT_DETOUR_START_ORDER | **C** | 🔗 **WG/AWG-endpoint с `detour` навсегда мёртв, если провайдер detour объявлен в конфиге ПОЗЖE потребителя.** Резолв detour утекал в конструктор `NewEndpoint` (egress-anchor-каст SPEC 020 идёт по `Upstream()`, `DetourDialer.Upstream()` жадно резолвит) — в фазе Create, до сборки графа. Endpoints создаются в порядке массива, регистрируются в конце своего Create → провайдер, объявленный позже, ещё не в реестре → `sync.Once` кэширует `outbound detour not found` навсегда, нода не шлёт ни байта. Смена порядка чинила случайно. Ядро упорядочивает старт по зависимостям (топосорт `startOutbounds`, detour = `Dependencies()`), но резолв утекал из-под барьера. Фикс (1 файл): (A) каст egress-anchor обёрнут в `if Detour==""` — на detour-пути он и так всегда `false`; (B) `InitializeDetour` в `Start(StartStateStart)` за топосорт-барьером → `not found` = fail-fast, без ретраев. Red/green e2e (потребитель раньше провайдера): RED ~92с таймаут, GREEN ~2.8с. Остаток: field. [SPEC.md](TASKS/029-ENDPOINT_DETOUR_START_ORDER/SPEC.md) |
| **030** | FAST_BOX_SHUTDOWN | **C** | ⏱️ **`box.Close()` виснет 10с+ на Android при остановке ~30 пропинганных WG/AWG-нод.** `box.Close` рвал endpoints, пока idle/urltest-тик ещё слал wake-пинги → каждый `Endpoint.Close()` блокировался на `resumeMu`, ожидая, пока пинг-разбуженный `resumeOnDial` доделает полный device-rebuild + handshake (~0.5–5с), суммируясь последовательно по всем нодам. Аудит (3 агента+синтез): доминанта — resumeMu×wake-сумма, НЕ worker-drain. Фикс (4 шага, drain НЕ трогаем — иначе use-after-free gVisor netstack): (1) `Router.QuiesceForShutdown` в начале `box.Close` — стоп тика + `DevicePause` broadcast закрывает все UDP-сокеты вперёд; (2) гейт `closing` в WG-endpoint прерывает in-flight wake; (3) сокеты закрыты → `stopping.Wait` мгновенный; (4) параллельное закрытие (`task.Group` Concurrency 8, все джойнятся). 10с+ → доли секунды, ничего не бросается недозакрытым. Юниты гейта red/green + e2e-smoke (20 нод ~5мс). Дополняет клиентский таймаут LxBox вокруг `closeService()`. Остаток: field. [SPEC.md](TASKS/030-FAST_BOX_SHUTDOWN/SPEC.md) |
| **031** | AWG_PARITY_AUDIT_ADVANCED_SECURITY | **C** | ✅ **Клиентский паритет AWG полный — реализовывать нечего.** Ответ на «все ли фишки AWG мы понимаем/реализовали». У AmneziaWG **нет нормативной спеки** (ни RFC, ни whitepaper): протокол задан реализациями, поэтому сверка — по `amneziawg-tools` (`src/config.c` — что официальный инструмент принимает в `.conf`) с кросс-проверкой netlink-контракта ядерного модуля. Совпадение полное: `Jc/Jmin/Jmax`, `S1–S4`, `H1–H4`, `I1–I5`, CPS-мини-язык 1:1 (8 тегов `b,d,ds,dz,r,rc,rd,t`); интроспекция (`IpcGet`) тоже отдаёт все 16 — закреплено round-trip-тестом. **`AdvancedSecurity`** — не зазор, а **серверное** поле: device-уровень выводится автоматически из наличия junk-параметров (в `false` не ставится нигде), peer-уровень пишется при разборе **входящего** handshake (`mh_validate` magic'а пришедшего пакета) и уходит в уведомление о неизвестном пире; на send-пути отсутствует. В клиентском `[Peer]` (описывает сервер) смысла не имеет. **Отозвано три гипотезы:** `J1–J3`/`Itime` не существуют ни в одной реализации; «advanced»-ключи ветки `master` `amneziawg-go` (`header_protection_key`, `content_padding_addition`, 5 таймингов) — неreleased разработка (tools не шлёт, модуль не знает) ⇒ `master` одной реализации ≠ протокол; «get-путь не отдаёт `i1..i5`» — ложная находка грепа по литералам (эмитятся циклом `i%d=`). Методический вывод: грep по литералам не доказывает отсутствие — проверять прогоном. [SPEC.md](TASKS/031-AWG_PARITY_AUDIT_ADVANCED_SECURITY/SPEC.md) |
| **032** | VLESS_ENCRYPTION_MLKEM768 | **C** | 🔐 **Клиентская поддержка `encryption: mlkem768x25519plus…` — пост-квантовый слой ВНУТРИ VLESS (не key exchange REALITY).** Диагностирован по живой жалобе: 8 noTLS-нод подписки мертвы у нас и живы в NekoBox+ — дамп его базы под root (`sager_net.db`) показал у тех же серверов `mlkem768x25519plus.native.0rtt.<ML-KEM-768>`, а у соседней записи ошибку `encryption handshake:`. Симптом: WS-апгрейд проходит (`101`), дальше сервер рвёт связь, в логе ядра пусто. **Отклонение от плана:** вместо реализации с нуля по PR Xray (MPL-2.0) клиентская часть портирована из `starifly/sing-box` (`protocol/vless/encryption`) — тот же GPL-3.0 и та же upstream-база ⇒ снялись разом вопрос лицензии, нужда в эталонном стенде и главный риск «ошибка в HKDF = молча не коннектится». Сервер (`decryption`) не брали (client-only). Врезка ровно как планировалась: 2 upstream-файла, остальное в lx-файлах; новых внешних зависимостей нет. Юниты парсера 14 случаев, `check` принимает реальную spec-строку. **ДЕВАЙС-ВЕРИФИЦИРОВАНО: +10 нод** (ws 0/8→6/8, grpc 0/5→4/4, прочие группы не сдвинулись ⇒ эффект чисто от слоя); синхронно доработан билдер LxBox, который терял поле (в подписке оно в `users[0].encryption`, в конфиге ядра — плоское поле аутбаунда). Остаток: непокрытые формы (`1rtt`+padding, `xorpub`/`random`). [SPEC.md](TASKS/032-VLESS_ENCRYPTION_MLKEM768/SPEC.md) |
| **033** | DNS_GROUP_SERVER | **C** | 🧭 ✅ **v2: TTL-модель, код+тесты+DoD+адверс-ревизия (24 агента, 6 уник. находок исправлены — nil-веер, дырявая Reset-амнистия, provisional-current и др.).** Режимы `stable`/`fastest`/`parallel` как грани одного механизма (034 слита сюда по §3.2, номер свободен); две TTL-записи (ошибка/победа, успех↔ошибка симметрично стирают), веер по чистым с гарантированным бюджетом, анти-шторм «чистых нет → одна попытка», single-flight выборов. v1 (failover/race/down_time, отгружена в rc.1) — в HISTORY.md; конфиг rc.1 сознательно ломается (потребителей нет). [SPEC.md](TASKS/033-DNS_GROUP_SERVER/SPEC.md) |
| **035** | DNS_GROUP_OBSERVABILITY | **C** | 👁 ✅ **v3: код+тесты+DoD** (036 слита сюда по §3.2, номер свободен)**.** `racer`→`fanned` + новый `survival` (видимость деградации), state-RPC v3 (`clean`/`liveErrors`/`lastErrorAgeMs`/`liveWins`/`current` вместо winner/ranking/down-полей — замена, v2-прото не был в релизе). Механизм трассы (write-once effective, path изнутри наружу, снимок под мьютексом) без изменений. [SPEC.md](TASKS/035-DNS_GROUP_OBSERVABILITY/SPEC.md) |
| **037** | RUNNING_CONFIG_RPC | **C** | 📄 ✅ **Код+тесты+DoD.** `GetRunningConfig` — канонический JSON options, из которых реально построен работающий box (post-override: tun AutoRedirect/packages, OOM-killer). Снапшот сериализуется один раз в `newInstance` (энкодер `FormatConfig`), хранится строкой в `Instance`, захват за `with_lx_command` (безтеговая сборка = upstream по поведению и памяти). Закрывает окно рассинхрона профиль↔ядро в LxBox: «View details»/«Copy JSON» узла выводятся на клиенте из этого документа (per-tag RPC и `json`-поле в `GroupItem` отвергнуты — см. PLAN). Re-marshal ≠ байт-в-байт исходник → сравнение только семантическое. Attached-путь (`service/api`) снапшота не имеет → `Unavailable`. Документ отдаётся объектом с `Content()` — форма возврата вынужденная, см. [038](TASKS/038-GOMOBILE_STRING_RETURN_FRAME_KILL/SPEC.md). Остаток: field из LxBox после AAR. [SPEC.md](TASKS/037-RUNNING_CONFIG_RPC/SPEC.md) |
| **038** | GOMOBILE_STRING_RETURN_FRAME_KILL | **C** | 💀 **Метод libbox с голым `string` убивал ядро на android/arm64.** `GetRunningConfig` (037) был единственным экспортированным методом `CommandClient` с `(string, error)`; gomobile кодирует строку в `nstring{void*;len}`, cgo кладёт её в `__packed__`-фрейм (теряется 8-выравнивание, на arm64 адрес ≡4 mod 8), Go пишет указательный слот через write barrier → `fatal error: bulkBarrierPreWrite: unaligned arguments`, туннель падает без recovery. Убито в `v1.14.0-lx.16-rc.3` и stable `v1.14.0-lx.16`. Фикс: возврат `*RunningConfig` + `Content()` (фрейм без указателя, как у `Rule`/`PoolSlot`). ⚠️ `[]byte` НЕ лечит — `nbyteslice` та же форма. Guard — reflection-тест по всей поверхности `CommandClient`. Ломающее изменение libbox API. [SPEC.md](TASKS/038-GOMOBILE_STRING_RETURN_FRAME_KILL/SPEC.md) |
| **039** | REPORT_ARCHIVE_ROTATION | **C** | 🗄 **Архивы отчётов апстрима растут без предела.** `oom_reports`/`crash_reports`: каждое событие — новая папка (~750 КБ: два pprof + копия конфига), удаления нет ни на одном из двух путей записи. На устройстве — **575 папок / 427 МБ за 19 дней**, пик 94 отчёта в сутки. Фикс: `pruneReports` в общем `report.go` + вызов в `WriteReport` и `archiveCrashReport`; лимиты 32 папки / 64 МБ (действуют оба, подрезка до `keep-1`). Порядок удаления **по mtime, не по имени** — суффиксы коллизий (`…-05-2` vs `…-05-10`) ломают лексикографику. Best-effort: путь краша, ошибки глотаются. Все три файла — чистый upstream, вставки под `// lx:begin report-rotation`. Накопленное фикс сам не чистит. [SPEC.md](TASKS/039-REPORT_ARCHIVE_ROTATION/SPEC.md) |
| **040** | SINGTUN_ACCEPTLOOP_SELFHEAL | **C** | 🩹 **System-стек TCP умирал навсегда, если его листенер закрыли из-под ядра.** При `stack: "system"` каждое новое TCP-соединение из TUN NAT-переписывается на локальный форвардер; апстримный `acceptLoop` считал **любую** ошибку `Accept` терминальной и молча выходил — стек продолжал работать и переписывать каждый SYN на мёртвый порт, ОС отвечала мгновенным RST. Каждое приложение получало `ECONNREFUSED` за ~16 мс до перезапуска VPN, при живых UDP/QUIC/DNS. Воспроизводилось ~1 раз на 8–36 быстрых рестартов, чаще на «грязном» процессе — оттого месяцами не ловилось. Фикс — самолечение в форке сабмодуля `sing-tun`: цикл переоткрывает листенер вместо тихой смерти. ⚠️ Механизм уточнён по устройству: триггер — `reload`, `errno=EINVAL` значит сокет **разлушен, а не закрыт** (искать `shutdown`, не `close`); `fdsan` тут бесполезен. Device-верифицировано (два живых восстановления). Отгружено в `v1.14.0-lx.17-rc.4`. [SPEC.md](TASKS/040-SINGTUN_ACCEPTLOOP_SELFHEAL/SPEC.md) |
| **041** | WG_HANDSHAKE_GIVEUP_REBIND | **C** | 🩹 **WG/AWG-узлы после сна навсегда в ERR, лечил только реконнект (dewch #1298+).** Дампы: `StdNetBind`-сокеты живы 33 мин, порт не менялся — per-flow-состояние пути (NAT/DPI 5-tuple) умерло, а `wireguard-go` после give-up (90 с ретраев) вечно ретраит в мёртвый сокет. v1 (rc.4, ✅ synthetic + uphold, живые `BindUpdate` в полевом дампе 01.08): пассивный self-heal в форке submodule — событие give-up один раз переоткрывает bind (свежий ephemeral-порт, если `listen_port` не пинован; дебаунс 90 с; на down/closed девайсе — no-op, совместимо со сном 020) и сразу кикает новое рукопожатие; оба bind-пути одним механизмом. v2 (✅ synthetic + uphold 6/6): полевой остаток v1 — лечение лишь на ~90-й секунде, юзер меряет пинг в первые полминуты после пробуждения и видит ERR — закрыт двумя триггерами того же rebind: досрочный (≥3 неотвеченных инициации при доказуемо мёртвой сессии — нет keypair / handshake старше 180 с → ~15 с вместо 90) и событийный нудж libbox `RebindStaleEndpoints()` (LxBox дёргает на `USER_PRESENT`; здоровые/спящие узлы — no-op). Дебаунс общий, конфиг не расширяется. Полевой остаток: нудж на стенде жалобы (нужен AAR + `USER_PRESENT`-ресивер в LxBox). См. HISTORY.md. [SPEC.md](TASKS/041-WG_HANDSHAKE_GIVEUP_REBIND/SPEC.md) |
| **042** | XHTTP_STREAM_GRPC_CONTENT_TYPE | **C** | 🏷 **Не слали `Content-Type: application/grpc` на потоковых запросах — расхождение с Xray.** Bug в 002 (жалоба: 14 XHTTP-нод подписки мертвы, те же ноды живы в Xray-клиенте). Xray ставит gRPC-тип на любом запросе с телом (`FillStreamRequest`); сам XHTTP-сервер его не читает — предполагалось, что заголовок нужен CDN/обратному прокси перед нодой для потоковой отдачи ответа (**гипотеза не подтвердилась**, см. ниже). Смотрели на `mode:auto`: разрешение `auto` у нас корректно (сверено с Xray v26.7.28 — REALITY→`stream-one`, эвристику не убирали), но вело в сломанную ветку. Опция `no_grpc_header` числилась заглушкой «has no effect» на ложной посылке — стала действующей. Девайс-матрица (менялся только режим): `auto`/`stream-one` → все `-1`, `packet-up` → все живы 142–216 мс. Фикс: `applyGRPCHeader` в `stream-one`+`stream-up`, юнит на три случая. Досталось от [011](TASKS/011-XHTTP_STREAM_ONE_DOWNLINK/SPEC.md) — та принята на синтетике, лайв был открытым TODO. ⚠️ **Лайв с пересобранным ядром картину НЕ изменил** (0/14 как было) — паритет с Xray восстановлен, но жалоба-триггер не закрыта. **Настоящий корень найден позже — [043](TASKS/043-XHTTP_STREAM_ONE_PATH_PREFIX/SPEC.md)** (срезанный слэш в пути `stream-one` → 404). Механизм «буферизующий прокси» так и остался неподтверждённой гипотезой: фикс ценен как паритет с Xray, не как лечение. [SPEC.md](TASKS/042-XHTTP_STREAM_GRPC_CONTENT_TYPE/SPEC.md) |
| **043** | XHTTP_STREAM_ONE_PATH_PREFIX | **C** | 🎯 **КОРЕНЬ жалобы «XHTTP-ноды подписки мертвы» — срезанный слэш в пути `stream-one`.** Сервер (Xray/NekoBox) нормализует свой путь в `<path>/` (когда session/seq в пути — умолчание) и **404-ит всё, что не начинается с этого префикса**. Мы в `stream-one` слэш срезали (наследие [011](TASKS/011-XHTTP_STREAM_ONE_DOWNLINK/SPEC.md): вместе с sessionId убрали и слэш) → `POST /api/v1/feed` против ожидаемого `/api/v1/feed/` → 404 → зависание до таймаута без ошибки. `packet-up` жил, т.к. его путь продолжается в `/<uuid>` и префикс проходит; `auto`+REALITY уходит в `stream-one` — отсюда ложный след «сломан auto». Доказано на проводе: до фикса `REJECT 404 lacks prefix`, после — `ACCEPT`. Фикс: `barePathForStreamOne` (слэш при path-размещении, verbatim иначе), sessionId по-прежнему пустой. Два юнита закрепляли ошибочное ожидание — обновлены, +regression-guard. Диагностика: сторонний клиент поднимал те же ноды за 34 мс на том же устройстве ⇒ баг наш; hy2 0/11 той же подписки — НЕ наш (UDP режется каналом, в чужом клиенте без туннеля тоже тайм-аут). **Девайс-верифицирован, отгружен в стабильном `v1.14.0-lx.17`.** [SPEC.md](TASKS/043-XHTTP_STREAM_ONE_PATH_PREFIX/SPEC.md) |
| **044** | ANDROID_AAR_GO124_QUIC_DEAD | **C** | 🧰 **AAR, собранный Go 1.24.x, убивал все quic-go-аутбаунды (hysteria2/tuic/masque-h3) на вендорских Android-ядрах** — каждый dial внутри VPN-процесса виснет до `context deadline exceeded`; TCP-протоколы и wireguard-go живы, generic-эмулятор ложно-зелёный. Изолирован на железе A/B-парой AAR go1.24.7 ❌ / go1.25.5 ✅ (единственная переменная — тулчейн; порог = go1.25.0 GA по бисекту, см. RESEARCH.md). Фикс — среда сборки, не код: пин `go-version` во всех трёх AAR-джобах CI (сначала 1.25.x, затем 1.26.x); `go.mod` остаётся апстримным. Попутно — Debug-ручки `SetQuicGSODisabled`/`SetQuicECNDisabled` (offload-класс исключён на устройстве). Отгружен в `v1.14.0-lx.19-rc.2`. [SPEC.md](TASKS/044-ANDROID_AAR_GO124_QUIC_DEAD/SPEC.md) |
| **045** | TLS_DISABLED_NIL_DIALER_CRASH | **C** | 💥 **Trojan/VLESS-нода с `"tls": {"enabled": false}` роняла весь процесс** nil-паникой на первом дозвоне (вкл. URL-тест) — регрессия апстримного ECH-коммита `1f0308054` (dialer создаётся под гейтом `options.TLS != nil`, а конструктор конфига для `enabled:false` по контракту отдаёт `(nil, nil)`); жива в upstream `testing`. Диагностирован по крашбандлу 4PDA (порт в кадре стека = единственная `enabled:false`-нода конфига); стреляет только при удавшемся TCP — нода-мина молчит, пока ТСПУ дропает её коннект. Фикс: nil-гейт как у vmess в оба конструктора (`// lx: tls-disabled-dialer`), red/green юниты обоих пакетов. Условие снятия — в реестре [HOTFIXES](FEATURES/004-HOTFIXES/FEATURE.md). Остаток: field после AAR. [SPEC.md](TASKS/045-TLS_DISABLED_NIL_DIALER_CRASH/SPEC.md) |
| **046** | DNS_HIJACK_PACKET_LOOP_STALL | **C** | 🧊 **Один DNS-сервер с `detour` на мёртвый outbound замораживает весь форвардинг туннеля** — DNS чужих серверов, ICMP, новые соединения; ядро живо и молчит, через 4–5 мин «само» оживает и снова умирает. Hijack-DNS ставится **синхронно из пакетного цикла стека** (system `ForwardDispatcher` и gvisor `dispatchLoop` — оба), а `Client.ExchangeAsync`, вопреки имени, блокирует вызывающего в `ConnPool.acquireShared` до DNS-таймаута (10 с), пока dial сервера висит на мёртвом detour (DPI silent-drop / TEST-NET). Каждый уникальный запрос = 10 с заморозки цикла; фонового потока ru-запросов Android-приложений хватает, чтобы цикл стоял почти всегда. Смертельный стек снят живьём на эмуляторе (`dispatchLoop → … → HijackDNSPacket → … → acquireShared select`). Клиентский симптом (LxBox, 2026-08-02): «первая страница грузится, дальше всё висит / сменилась сеть / 0 соединений», конфиг легальный (`yandex_udp` detour vpn-2, в vpn-2 дохлая нода). Фикс: постановка exchange в горутину под семафором 256, сверх лимита — дроп (`// lx: dns-hijack-async`, `route/dns.go`+`route/router.go`), юниты зелёные. Эмуляторная верификация пройдена, срезан в `v1.14.0-lx.20-rc.2`; остаток — device-подтверждение на конфиге инцидента. [SPEC.md](TASKS/046-DNS_HIJACK_PACKET_LOOP_STALL/SPEC.md) |
| **047** | EARLY_RPC_NIL_ROUTER_CRASH | **C** | 💥 **Смена сети в момент старта туннеля роняла весь процесс** nil-паникой в `NetworkManager.ResetNetwork` (`r.router == nil`). Гейт RPC проверял `Box() != nil`, но `s.instance` публикуется **до** `instance.Start()` ([daemon/started_service.go:214](../daemon/started_service.go)), а поля `NetworkManager` присваиваются только на `StartStateInitialize` — уязвимо всё время `Box.Start()`, не только его начало. `preStart` лишь расширяет окно (bbolt-транзакция `cache.db`); обе стороны гонки сняты одним крашдампом (`1.14.0-lx.19-rc.3`, Telegram 2026-08-03). Стреляет штатный авто-`resetNetwork()` на WiFi↔LTE (LxBox §087 регистрирует монитор до старта сервиса). Проверены все четыре метода со слабым гейтом — доказан **один** (`ResetNetwork`); `NeedWIFIState`/`UpdateWIFIState`/`NeedFindProcess` nil-safe и приведены к общему гейту как гигиена. Фикс: ранний nil-guard в `ResetNetwork` (до `CloseAll()`) + предикат `Ready()` (`ServiceStatus_STARTED` — тот же паттерн, что у `URLTest` и всех lx-RPC), экспортированный из `daemon` новым lx-файлом. Red/green юниты + `-race`, обе сборки libbox зелёные. Остаток: field после AAR. [SPEC.md](TASKS/047-EARLY_RPC_NIL_ROUTER_CRASH/SPEC.md) |
| **048** | GVISOR_HANDSHAKE_NIL_CRASH | **C** | 💥 **TCP-соединение, не дошедшее до established, роняет весь процесс** nil-паникой в gvisor: `handleConnecting` зовёт `ep.h.processSegments()` на endpoint'е с уже занулённым `h`. `performHandshake` в ветке провала ставит `ep.h = nil`, отпускает мьютекс и только потом идёт в `ep.Close()` — а состояние endpoint'а в этом окне ещё `SynSent`/`SynRecv`, поэтому гейт `handleConnecting` (проверяет состояние, но не `h`) пропускает пришедший сегмент. Обе стороны гонки сняты одним крашдампом (`1.14.0-lx.19-rc.3`, Telegram 2026-08-03): паникующая горутина и стоящая на `accept.go:323` держат **один и тот же** адрес endpoint'а. Баг апстримный, но наш путь расширяет окно — lazy-режим sing-tun дёргает `CreateEndpoint` отложенно, из сниффера (`PeekStream` → `SetReadDeadline`), т.е. уже из роутинг-пайплайна. Триггер — недостижимый узел (молчит/RST) при живых ретрансмитах SYN. Фикс: nil-guard на `ep.h` в начале `handleConnecting` (закрывает все 5 разыменований разом). **Баг воспроизведён прогоном** на самом пине зависимости — стек совпал с крашдампом построчно (`connect.go:534` / `dispatcher.go:152`), с guard'ом red→green, сборка и `vet` чистые. Защита на нашей стороне невозможна: паника рождается во внутренней горутине gvisor (`go p.start`), `recover()` в чужой горутине не работает, а окно открывается между `ep.mu.Unlock()` и `LockUser()` внутри `Close()` — обе точки в зависимости; наша единственная точка входа (`gLazyConn.HandshakeContext`) к тому моменту уже за окном. Подключён третий форк-сабмодуль `submodules/gvisor` ([Leadaxe/gvisor-lx](https://github.com/Leadaxe/gvisor-lx)) — **снапшот пина без истории**: вся тяжесть апстрима оказалась в истории (1.45 ГБ), рабочее дерево — 7.3 МБ / 712 файлов, `.git` 4.1 МБ, то есть порядок существующих сабмодулей. Дельта: 12 строк guard + 45 строк теста; тест едет вместе с патчем, поэтому его потеря при переносе на новый пин даст красный, а не тишину. Остаток: стенд «чёрная дыра» + field. [SPEC.md](TASKS/048-GVISOR_HANDSHAKE_NIL_CRASH/SPEC.md) |
| **049** | GO_TOOLCHAIN_PIN_FILE | **C** | 🧰 Единый пин версии Go-тулчейна в файле `go.version` — его читают 15 шагов `setup-go` в пяти `lx-*.yml`. Не `go-version-file: go.mod` (даёт 1.24.7 = регрессия 044). [SPEC.md](TASKS/049-GO_TOOLCHAIN_PIN_FILE/SPEC.md) |
| **050** | URLTEST_ZOMBIE_RUN_SURVIVES_RESTART | **O** | 🧟 **URL-тест на полуживом узле `vless + xhttp + encryption` виснет навсегда и переживает остановку ядра.** Три дефекта, смертельных только вместе: (1) у XHTTP-conn нет дедлайнов — `streamConn`/`splitConn` возвращают `os.ErrInvalid` на все `Set*Deadline`, а `Write` идёт в `io.Pipe`, который никто не читает, пока `RoundTrip` (запущенный отдельной горутиной) не поднял поток; (2) `encryption.Handshake` работает с голым `net.Conn` **без ctx** и вызывается уже после создания conn — поэтому отмена не доходит, и одиночный `URLTestOutbound` виснет вопреки корректному per-call ctx с таймаутом; (3) `URLTestGroup.Close()` гасит только ticker и не отменяет идущий прогон, а `batch.Wait()` = чистый `wg.Wait()` и отмену ctx **не слушает вовсе** — значит уровень 3 в одиночку ничего не чинит. Полевой дамп (Android arm64, `1.14.0-lx.17-rc.3`): два прогона `testNodes` возрастом 100 и 43 минуты пережили полный Stop → Start, держат массив на 2806 узлов при 592 в конфиге; 6 горутин на одном стеке (25–98 мин), 5 OOM-снимков 396–428 МБ; группа перестала публиковать замеры («утерялся пинг»), лечил только ребилд конфига. Механизм подтверждён по коду; узлы пользователя не нужны — стенд = listener, принимающий TCP и не читающий тело. [SPEC.md](TASKS/050-URLTEST_ZOMBIE_RUN_SURVIVES_RESTART/SPEC.md) |

| **051** | UPSTREAM_MERGE_235 | **O** | 🔄 Дрейф от `upstream/testing` — **235 коммитов** — влит (`f56680e0d`), merge-base == tip. 65 конфликтов разрешены по категориям; `*.pb.go` регенерированы через `lx-proto`, не слиты. **Опаснее конфликтов оказалось АВТОслияние**: три файла git склеил без конфликта и сломал (потерянный импорт `time` для `IdleSuspendable`, задвоенная `logRefreshedResponse`, мёртвый `return e.tunDevice.Close()` после нашего guard'а → nil-паника в тесте SPEC 020) — ловится только сборкой и тестами. Второй урок: «нет lx-маркера» ≠ «файл не наш», сверять по авторству коммитов. Блокер релиза (tailscale 1.92→1.102 требовал `PeerLookupFunc`/`NewPeerConfig`/`SetPeerLookupFunc` из свежего wireguard-go, которых не было в базе AWG-форка ⇒ ядро зелёное, AAR сломан) снят тремя cherry-pick'ами в сабмодуль вместо полного re-graft; AWG-обфускация цела. Отсюда новое правило релиза: **форк-сабмодули проверяются ДО мержа ядра** (раздел 1 раннбука). Остаток: device-прогон. [SPEC.md](TASKS/051-UPSTREAM_MERGE_235/SPEC.md) |
| **052** | NETSTACK_CONNECT_DEADLINE | **C** | ⏱️ **TCP-дайлы через gVisor-netstack (WG/AWG+MASQUE, openvpn, openconnect, tailscale) были единственным классом путей дайла без таймаута** — на тихой чёрной дыре каждый дайл молча висел ~127с SYN-бэкоффа gVisor (1+2+4+8+16+32+64; домены — до N×127с), группе не на что реагировать, приложения ретраят в тот же мёртвый узел. Полевой дамп (CPH2411): 16 дайлов припаркованы в `DialTCPWithBind`. Апстрим-дизайн (файл байт-в-байт), не lx-регрессия; `TCPSynRetriesOption` в пине gVisor — мёртвая ручка. Фикс: одноразовый connect-дедлайн `C.TCPTimeout` (15s = бюджет проб, ловушка S5) в leaf-швах — единственных местах, где дедлайн не переживает connect (класс SPEC 050); per-address бюджет в DialSerial бесплатно; UDP сознательно не тронут; SPEC 020 cold-wake в бюджет не входит (rebuild до leaf-дайла). Стенд: blackhole `2m7.065s`→**`15.050s`**, warm/wake не задеты; юнит рубит blackhole-connect за 0.3с по ctx. Исследование природы (7 агентов, замеры E1–E3, реестр ловушек S1–S6) — [RESEARCH.md](TASKS/052-NETSTACK_CONNECT_DEADLINE/RESEARCH.md). Реакция группы на ошибки — SPEC 054. [SPEC.md](TASKS/052-NETSTACK_CONNECT_DEADLINE/SPEC.md) |
| **077** | XHTTP_DIAL_CTX_CONTRACT | **I** | 🧵 **DNS-серверы `udp`/`tcp`/`tls` с `detour` через XHTTP-узел (`stream-one`/`stream-up`) молча падали `write request: context canceled`** — красные URL-тесты masque/WG по доменным URL (резолвят через DNS ядра) и мёртвый системный DNS через TUN при живом туннеле; DoH через тот же узел работал. Связка двух штатных механизмов: апстримный пул DNS-транспорта отменяет dial-контекст сразу после возврата `dial` (контракт `net.Dialer`), а сторож SPEC 050/072 в `dialStreamOne`/`dialStreamUp` слушал dial-контекст и ПОСЛЕ возврата — до `created`, который в `stream-one` приходит лишь после первой записи. **Никогда не работало** (до 072 запрос ездил на dial-контексте), а `auto`+REALITY = stream-one — бьёт дефолтный конфиг. Фикс: `DialContext` паркуется до принятия тела запроса HTTP-слоем (первый *вызов* `Read` на пайпе, не возврат — иначе дедлок 061), `created`, провала raise или конца dial-контекста; download-ответ не ждётся (061 цел); сторож снят; провал raise до принятия тела и полуживой узел на холодном пуле — ошибка из dial по дедлайну вызывающего (072-C штатно). Ловушки, найденные при реализации: double-release xmux-слота (`fail()` + error-path `DialContext` — единый once-хэндл на весь dial); приоритет `adopted` над `ctx.Done()`. Сознательная цена: тёплый h2-conn к молча умершему пиру принимает тело мгновенно — ловится `ReadIdleTimeout`/041/read-deadline, а не 15-с дедлайном dial. Red/green: 4 новых теста красные на базе, `-race` пакета + WG/DNS-сюиты зелёные. Остаток: полевой прогон на стенде лаунчера. [SPEC.md](TASKS/077-XHTTP_DIAL_CTX_CONTRACT/SPEC.md) |
| **078** | WIREGUARD_PACKET_SNIFFER | **I** | 🔍 **Сниффер `wireguard`** (FEATURE 016-SNIFF). Апстримный uTP-сниффер принимает любой пакет `01 00 …` от 20 байт, и plain-WG handshake initiation (148 байт) с LAN-устройства роутер помечал как `bittorrent` — так поток попадал в bittorrent-правило. Новый packet-sniffer по типу и размеру датаграммы (148/92/64, transport `>=32` и `(len-32)%16==0`, резервные байты свободны — WARP) стоит в дефолтном порядке перед uTP; имя `wireguard` доступно в `sniffer` и `protocol`. Апстримный файл не тронут; AWG с не-дефолтными H/S не распознаётся намеренно. Юниты + живой прогон через бинарь. Остаток: полевой прогон на роутере. [SPEC.md](TASKS/078-WIREGUARD_PACKET_SNIFFER/SPEC.md) |
| **079** | VPN_VOIP_PACKET_SNIFFERS | **I** | 🔍 **Снифферы `openvpn` / `ike` / `tailscale` / `sip`** (FEATURE 016-SNIFF, решение владельца 2026-09-05). Первый пакет клиента однозначен по форме: OpenVPN hard-reset (UDP и TCP за 2-байтной длиной; plain/tls-auth/tls-crypt), ISAKMP IKE_SA_INIT и IKEv1 main/aggressive на 500/4500, Tailscale disco по магии `TS💬`, SIP request-line с `Domain` из Request-URI (decoy `ip=sip` из 009 получает честное имя). RTP/L2TP/PPTP/KCP/VNC/обфусцированные — отвергнуты. Юниты + перекрёстный тест дефолтного порядка + прогон всех четырёх через бинарь; общая страница `docs-lx/lx-sniff{,.ru}.md`, §11 в lx-config. Остаток: полевой прогон на роутере, changelog при срезе тега. [SPEC.md](TASKS/079-VPN_VOIP_PACKET_SNIFFERS/SPEC.md) |
| **080** | AWG3_HEADER_PROTECTION_TIMINGS | **C** | 🛡 **AmneziaWG 3.0/3.1** — владелец получил тестовый сервер `amnezia-awg2` (`protocol_version 3.1`), которого AWG2-прививка не понимала: сервер шифрует заголовок каждого пакета (`HeaderProtectionKey`, ChaCha20, nonce = первые 12 байт паддинга `s1–s4`), плюс `ContentPaddingAddition`, `RandomTrailers`, `DisableCookies`, диапазонные тайминги и диапазонный `PersistentKeepalive`. SPEC 031 §3 («v3-ключи — неизданная ветка») устарела: выпущено как amneziawg-go v3.0.0…v3.1.20260828 / tools v3.1.20260812 — баннер поставлен. Порт диффа `v0.2.19→v3.1` в сабмодуль (in-place раскладка `[s4][header][sealed]`, `UintRange` вместо `magicHeader`, UAPI требует `s1–s4 ≥ 12` при ключе — и на частичном обновлении) + ядро (`option.AWGRange` для всех «число-или-диапазон» полей, 9 новых полей на корне endpoint, `persistent_keepalive_interval: "25-35"`, fail-fast в `check` с именем поля, гейт без `with_awg`). Нейтральные для провода отличия от референса: первый батч после старта переносится под актуальный `s4` (референс его теряет), UDP-окно сбрасывается по смене адреса, а не на каждый пакет; guard'ы SPEC 008/025 сохранены. Ловушка референса задокументирована: `random_trailers` × широкие `h1–h4` = ложная классификация data-пакетов. e2e с перехватом провода + `-race`; **DEVICE-VERIFIED**: живой сервер (хендшейк с первой попытки, HTTPS, 1 МБ, rekey по диапазону) и узел в лаунчере владельца (lx.33 под `lxd`, speedtest 35,67 Мбит/с; экспортный `mtu 1376` упёрся в path MTU ≈1390 за MASQUE-роутером — с 1280 работает). Выпущено в v1.14.0-lx.32. Остаток: импорт `vpn://` в лаунчер/LxBox с клампом MTU к 1280. [SPEC.md](TASKS/080-AWG3_HEADER_PROTECTION_TIMINGS/SPEC.md) |
| **081** | AWG_RECEIVE_INDEX_FIRST_CLASSIFICATION | **C** | 🛡 **AWG-приём: data-пакет с нашим живым receiver index классифицируется как transport ДО handshake-кандидатов.** Референсный порядок (init → response → cookie → transport) присваивал data-датаграмму по случайному слову типа: ровно `s1+148` байт у AWG2 (≈8 % таких пакетов при диапазонах из экспортов) и любой крупнее при `random_trailers` AWG 3.1 — пакет резался до размера handshake, не проходил MAC1 и терялся (downlink). Правка чисто приёмная: keystream шифра заголовка берётся на 8 байт (тип + индекс), новый первый кандидат по `indexTable` с живым keypair, промах — прежний порядок, провод не меняется; обратный риск ~10⁻¹⁰ и самовосстанавливается по `rekey_timeout`. Red/green e2e с детерминированной коллизией (`s1=44`, `s4=0`, `h1=5-4294967295`, 192-байтная датаграмма) в обе стороны. Uplink остаётся на приёмнике сервера — предупреждение в доках сохранено. [SPEC.md](TASKS/081-AWG_RECEIVE_INDEX_FIRST_CLASSIFICATION/SPEC.md) |
| **082** | H2_STREAM_ERROR_TYPE_LEAK | **B** | 🔥 **issue #14, корень:** conn'ы XHTTP / v2rayhttp / gRPC-lite отдавали наверх ошибку сброса стрима **типом** `http2.StreamError`; h2-клиент x/net поверх такого conn'а (DoH с `detour`, rule-set `download_detour`, chain) через залипание в crypto/tls принимал её за ошибку своего стрима и `continue`-ил в `readLoop` без единого сисколла — 100 % CPU до перезапуска. SPEC 076/077 к этому циклу не относились. Профиль `lx.30`: тел кадров 0.1 с из 36, `readRecordOrCCS` без детей, две readLoop в `runnable` на `transport.go:1886`. Фикс: `common/badh2.HideStreamError` — тот же текст, без `Unwrap`, в 6 точках XHTTP + `HTTP2Conn` + `GunConn`. Регрессия воспроизводит спин (13 млн чтений за 3 с без фикса). Остаток: полевой прогон на `lx.35`, конфиг репортёра. [SPEC.md](TASKS/082-H2_STREAM_ERROR_TYPE_LEAK/SPEC.md) |
| **083** | REALITY_MLKEM_KEYSHARE | **B** | 🔐 **REALITY-узлы на Xray ≥ v26.9.8 молча уходят на камуфляжный сайт (`reality verification failed`).** `XTLS/REALITY@8cdf7bf` требует key_share `X25519MLKEM768` перед X25519; апстрим sing-box сам вырезает гибрид из ClientHello (костыль коммита «Update utls to v1.7.2», когда utls не различала ключи гибрида и чистого X25519) — issue SagerNet#4520 без реакции. Фикс: фильтр снят (спека `HelloChrome_133` шлёт GREASE, MLKEM, X25519 как надо), `AuthKey` по `Ecdhe`, при nil — по `MlkemEcdhe` (порядок сервера, как у Xray/mihomo). Стенд на Mac: до патча против v26.9.9 все отпечатки падают, после — `chrome` 204; v26.7.28/v26.7.11 без регрессии на всех отпечатках. Не-Chrome отпечатки (firefox/safari/ios/edge/android/360/qq, `random` в 4 из 5, `randomized` ~½) гибрида в `metacubex/utls` не несут и против нового Xray мертвы (у Xray другая ветка utls — refraction с Firefox 148, паритета нет); `firefox` закрыт форком [086](TASKS/086-UTLS_FORK_FIREFOX148/SPEC.md), `safari` — [087](TASKS/087-UTLS_SAFARI_26_3/SPEC.md); `edge`/`ios`/`android`/`360`/`qq` — по решению владельца остаются, подмена на уровне приложений (LxBox §281). Тем же стендом закрыт полевой остаток SPEC 053 (v26.7.11: `26.3.27` ходит, `1.8.1` отсекается). Ловушка стенда: `dest = www.microsoft.com` ломает рукопожатие у REALITY до `8cdf7bf`. [SPEC.md](TASKS/083-REALITY_MLKEM_KEYSHARE/SPEC.md) |
| **084** | INTERRUPT_GROUP_ABBA_DEADLOCK | **B** | 🔒 **issue #20, корень:** переключение вложенного selector'а глушило весь трафик до рестарта (OpenWrt, `lx.35`: 981 goroutine, 67 в `sync.Mutex.Lock` внутри `common/interrupt`). `interrupt.Group` закрывал нижележащее соединение **под** своим мьютексом — в `Interrupt` и в трёх Close-обёртках; inbound-обёртка SPEC 064 v2 дала вложенным группам второй порядок захвата (входящее: внутренняя→внешняя, dial через `detour`: внешняя→внутренняя) → ABBA между `Interrupt` внутренней группы и `Close` DoH-соединения, после чего каждый `NewConn`/`NewSingPacketConn` встаёт на том же замке. **Наш регресс** с `lx.25` (у апстрима inbound-обёртки нет, порядок один). Фикс в примитиве: под замком только снятие записи, `Close` после `Unlock`; двойной `Close` допустим, как и раньше. Red/green: юнит на трёх видах обёрток с детерминирующим «затвором» и e2e на настоящих вложенных `Selector`'ах + `ConnectionManager`, `-race`. Остаток: полевой прогон на конфиге репортёра. [SPEC.md](TASKS/084-INTERRUPT_GROUP_ABBA_DEADLOCK/SPEC.md) |
| **085** | SOCKS5_UDP_ASSOCIATE_UNSPECIFIED_BIND | **C** | 🛰 **socks5-outbound: TCP ходит, UDP нет** (отчёт Cultsonfire 2026-09-14, LxBox 2.23.1 / `lx.34`: публичные прокси, часть через `detour` на MASQUE; в v2rayNG/Xray те же прокси по UDP работают, без MASQUE картина та же). Корень в sing (`protocol/socks/client.go:161-167`, v0.9.0…v0.9.3 и апстримный `dev`): после UDP ASSOCIATE клиент диалит релей по BND.ADDR из ответа как есть, а серверы, отвечающие `0.0.0.0`/`::`, получают ничего — для Go неопределённый хост означает локальную систему (документация `net.Dial`; на Mac dial `0.0.0.0:9` → `127.0.0.1:9`), ошибки нет. Xray подставляет адрес прокси (`proxy/socks/client.go:97-100`). Фикс в нашем слое, без форка sing и без копии его кода: `protocol/socks/udp_associate_lx.go` — `relayDialer`, обёртка dialer'а для `socks.NewClient`, перехватывает единственный UDP-dial (релей) и нормализует адрес (unspecified/пустой BND.ADDR → адрес сервера из конфига + порт из ответа; порт 0 — ошибка; адрес из конфига, а не `tcpConn.RemoteAddr()`, который под `detour` — пир плеча); один шов `lx:begin socks-udp-bind` в `NewOutbound` (+5 строк, только `version: 5`); хендшейк, отмена по ctx и таймауты остаются в sing. Red/green: записывающий dialer + страж на апстримный клиент (падает, когда sing починит сам) + контракт «релей диалится через переданный dialer» (падает, если sing начнёт диалить мимо) + живой round-trip через настоящий `NewOutbound`, `-race`. Выпущено в `lx.39`. Проверка на узлах репортёра: у выбранного BND `0.0.0.0` — фикс по адресу, но UDP-релей этого сервера мёртв для любого клиента (включая правило Xray), `urltest` выбирает по TCP — остаток жалобы серверный; путь TUN → socks UDP на AVD подтверждён STUN; через `detour` WARP MASQUE не проверено. [SPEC.md](TASKS/085-SOCKS5_UDP_ASSOCIATE_UNSPECIFIED_BIND/SPEC.md) |
| **086** | UTLS_FORK_FIREFOX148 | **C** | 🦊 **REALITY `fp=firefox` на Xray ≥ v26.9.8** — граница [083](TASKS/083-REALITY_MLKEM_KEYSHARE/SPEC.md): гибрид есть только в chrome-пресетах `metacubex/utls`. Четвёртый форк-сабмодуль `submodules/utls` = [Leadaxe/utls-lx](https://github.com/Leadaxe/utls-lx): metacubex `v1.8.7` + cherry-pick `fc716b2`+`ddebe39` из `refraction-networking/utls` (Firefox 148 и reuse одного X25519-ключа между гибридной и классической записями key share); единственный конфликт — блок import (`internal/mlkem` metacubex вместо `crypto/mlkem`), ожидавшийся конфликт в `ApplyPreset` не случился (регион идентичен). Структура ClientHello совпала с refraction (scratch-сравнение по сырым байтам); страж `common/tls/utls_firefox148_lx_test.go` (`firefox` = 148, гибрид перед X25519 у chrome/firefox, reuse + контракт `AuthKey` 083). Стенд на Mac 2026-09-16: Xray v26.9.9 — `fp=firefox` 204 (ядро до фикса — `reality verification failed`), v26.7.28/v26.7.11 и `chrome` без регрессии; оба AAR — dry run `lx-release` ✅; field ✅ на узле репортёра singbox-launcher#124 через лаунчер (lx.1 падал, lx.2 — 204). Выпущено в v1.14.1-lx.2. Остаток: AAR на AVD (LxBox после бампа пина), §281 для `firefox` — версией контракта D-119 синхронно с лаунчером. Issue [#22](https://github.com/Leadaxe/sing-box-lx/issues/22). [SPEC.md](TASKS/086-UTLS_FORK_FIREFOX148/SPEC.md) |
| **087** | UTLS_SAFARI_26_3 | **C** | 🧭 **REALITY `fp=safari` на Xray ≥ v26.9.8** — остаток границы [086](TASKS/086-UTLS_FORK_FIREFOX148/SPEC.md): `HelloSafari_Auto` в `metacubex/utls` v1.8.7 = Safari 16.0 без гибридного key share, клиент получает `reality verification failed`. Тот же приём: третий `cherry-pick -x` в форк [Leadaxe/utls-lx](https://github.com/Leadaxe/utls-lx) — `aa6edf4` «feat: add safari 26.3» из `refraction-networking/utls` (`HelloSafari_Auto = HelloSafari_26_3`, пресет +110 строк). Конфликтов не было: reuse ключа пресет не использует, зона `ApplyPreset` не затронута; ветка форка = тег `v1.8.7` + ровно три коммита. Структура ClientHello Safari 26.3 совпала с refraction (scratch-сравнение по сырым байтам; ECH GREASE в пресете нет — различий не осталось вовсе). Страж `common/tls/utls_firefox148_lx_test.go` расширен: `safari` = Safari 26.3, гибрид перед X25519 по разу у `chrome`/`firefox`/`safari` (5 тестов). Стенд на Mac 2026-09-16 (Xray на loopback, dest `www.apple.com`): v26.9.9 — `fp=safari` 204 ×3 (ядро lx.2 — `reality verification failed` ×3), v26.7.28/v26.7.11 — 204, `chrome`/`firefox` без регрессии. Решение владельца 2026-09-16: `edge`, `ios`, `android`, `360`, `qq` остаются — пресетов с гибридом для них нет ни у metacubex, ни у refraction (у Xray-core та же граница, паритет честный), подмена на уровне приложений. Выпущено в v1.14.1-lx.3. Остаток — как в 086: AAR на AVD. [SPEC.md](TASKS/087-UTLS_SAFARI_26_3/SPEC.md) |
| **088** | REALITY_FRAGMENT_BYPASS | **I** | ✂️ **REALITY-узлы игнорировали `fragment` / `record_fragment`** — апстримный `RealityClientConfig.ClientHandshake` строит `utls.UClient` на голом соединении, минуя обёртку `tlsfragment`, которую получают STD- и uTLS-клиенты: опции пишутся в конфиг и молча не действуют, а дефолт [060](TASKS/060-TLS_FRAGMENT_AUTO_ON_DETOUR/SPEC.md) (`record_fragment` под `detour`) до REALITY не доходил, хотя 060 обещала обратное. После [083](TASKS/083-REALITY_MLKEM_KEYSHARE/SPEC.md) заметнее: гибридный ClientHello = 1720–1885 байт, всегда два TCP-сегмента. Фикс: helper `UTLSClientConfig.wrapClientConn` — одна точка для uTLS- и REALITY-клиента; `ClientHandshake` разделён на `prepareClientHello` (сборка hello без отправки, тестируемо) + рукопожатие. Стражи: тип `NetConn()` по матрице флагов и проводной (`net.Pipe`: без флагов 1 TLS-запись, с `record_fragment` — 2); red-check пройден. Новых ключей нет. Не обещание обхода DPI: поможет или навредит — решает конкретная сеть, полевой прогон у репортёра LxBox #142 впереди. [SPEC.md](TASKS/088-REALITY_FRAGMENT_BYPASS/SPEC.md) |
| **089** | REALITY_KEY_SHARE_OPTION | **I** | 🔑 **`tls.reality.key_share`: `hybrid` / `classical`** — по-узловой выбор, нести ли в REALITY ClientHello гибрид `X25519MLKEM768`. Полевой отчёт 2026-09-17 (LxBox #142): на одном мобильном операторе двухсегментный гибридный ClientHello (`chrome` 1720 байт) теряется, апстримный (594 байта, один сегмент) проходит; у Xray-клиента тот же симптом (XTLS#6256, «not planned»). Что именно срабатывает — не доказано (второй телефон того же оператора и Chrome на этом же работают). `""` = как несёт отпечаток (поведение 083 без изменений); `classical` = апстримный фильтр по выбору узла, только Xray < v26.9.8; `hybrid` = проверка, что отпечаток шар несёт, иначе явная ошибка вместо тихого `reality verification failed` на `edge`; опечатка отвергается при построении. Реализация: Build → фильтр → Build (порядок апстрима до 083), `Ecdhe` остаётся — контракт `AuthKey` 083 не тронут. Автофолбэк не делается (сеть репортёра «наказывает» сервер ~минуту). Остаток: проводка в LxBox/лаунчер, стенд по 083 §2, полевой прогон вместе с 088. [SPEC.md](TASKS/089-REALITY_KEY_SHARE_OPTION/SPEC.md) |
| **090** | REALITY_SHORT_ID_OVERFLOW_PANIC | **I** | 💥 **REALITY `short_id` длиннее 16 hex-символов ронял процесс** — вместо ошибки конфигурации паника `index out of range [8] with length 8`: `sing-box check` падал без внятного сообщения, `run` / libbox `Start` / `lxd apply` убивали **весь** процесс (VPN, а не «узел с ошибкой»), inbound REALITY — при старте. Корень апстримный и ровесник REALITY (2023, в текущем `upstream/stable` дословно тот же): `hex.Decode` пишет `len(src)/2` байт в `dst`, не сверяясь с его ёмкостью, поэтому запись выходит за границы `[8]byte` **внутри** `hex.Decode`, а стоящая следом проверка `decodedLen > 8` мёртвая — управление до неё не доходит. Фикс: проверка `len(short_id) > 16` **до** декодирования в `common/tls/reality_client.go` и `common/tls/reality_server.go` (`// lx: SPEC 090`), текст ошибки апстримный (`invalid short_id`, на сервере с индексом) — на него ориентируются клиентские гарды; мёртвая проверка снята. Легальные значения не меняются: пусто = нулевой short_id, 1…16 hex; нечётная длина и не-hex по-прежнему `decode short_id`. Стражи `TestLxRealityShortIDTooLongRejected` / `TestLxRealityServerShortIDTooLongRejected` (`with_utls`), red-check пройден на обеих сторонах. Найдено DRIFT-инвентарём контракта LxBox на пине `v1.14.1-lx.4`; приложения (LxBox §343, лаунчер) такое значение отбрасывают сами, поэтому их пользователей не било. Условие снятия — в реестре [HOTFIXES](FEATURES/004-HOTFIXES/FEATURE.md): апстрим добавит проверку длины до `hex.Decode`; файлы апстримные — маркер проверять на каждом мерже. [SPEC.md](TASKS/090-REALITY_SHORT_ID_OVERFLOW_PANIC/SPEC.md) |
| **091** | CONFIG_VALIDATION_TUIC_MASQUE | **I** | 🧹 **Две дыры валидации конфига — опечатка вместо ошибки** (найдены DRIFT-инвентарём контракта лаунчера, DRIFT 131 §8.3; общий принцип [089](TASKS/089-REALITY_KEY_SHARE_OPTION/SPEC.md) — опечатка отвергается при загрузке, тихих дефолтов нет). **§1 `tuic.udp_relay_mode`** (апстримный файл, реестр [HOTFIXES](FEATURES/004-HOTFIXES/FEATURE.md)): в `switch` не было `default`, поэтому `"qiuc"`, `"Native"`, `"udp"` тихо означали `native`, тогда как соседний `congestion_control` при опечатке даёт ошибку — поведение расходилось внутри одного outbound'а. Теперь `unknown udp_relay_mode: <value> (expected native or quic)`; `""` по-прежнему `native`, проверка конфликта с `udp_over_stream` осталась выше. **§2 `masque` `profile: standard` без `uri`** (форк-нативный пакет, [021](TASKS/021-MASQUE_CONNECT_IP_OUTBOUND/SPEC.md)): `resolveVHTTP` стоял раньше проверки `uri`, поэтому при `vhttp: h2` пользователь получал честную, но не про то ошибку про h2 и, починив `vhttp`, упирался во вторую. Проверка `uri` поднята сразу за `resolveLegacyOptions` (legacy-поля могут его заполнять), текст расширен до самодостаточного — что писать в `uri`; ошибка про h2 на `standard` осталась, но до неё доходят только конфиги с `uri`. Провод, ключи и дефолты не меняются, `cloudflare` не тронут. Стражи `protocol/tuic/udp_relay_mode_lx_test.go` и `protocol/masque/standard_uri_lx_test.go`, red-check пройден на обоих. [SPEC.md](TASKS/091-CONFIG_VALIDATION_TUIC_MASQUE/SPEC.md) |
| **092** | INIT_ERROR_NAMES_TAG | **I** | 🏷️ **Ошибка конфигурации называет элемент по типу и тегу, а не только по индексу** (пожелание LxBox от 2026-09-18 после приёма lx.5). Было: `initialize outbound[0]: invalid short_id` — приложения (LxBox §455, лаунчер, `lxd apply`) показывают текст ядра дословно, а у пользователя в подписке сотня узлов с тегами, а не с номерами: по индексу он не находит, какой узел чинить. Стало: `initialize outbound[0] vless[proxy-de-1]: invalid short_id` — хвост ` <type>[<tag>]` повторяет уже существующее имя логгера (`outbound/vless[proxy-de-1]`), тип и тег в каждом цикле рядом и так вычислены. Правка в `box.go` (`// lx: SPEC 092`), шесть индексированных циклов инициализации: DNS server, endpoint, inbound, service, outbound, certificate provider. Границы: апстримный префикс `initialize <kind>[<i>]` сохранён **дословно** (на него могут матчить существующие парсеры), без явного тега хвост даёт `vless[0]` — тип всё равно полезен; ошибки **внутри** конструкторов (`invalid short_id`, `unknown udp_relay_mode: …`) не менялись, формат логгеров, Clash API, gRPC/lxd и libbox-сигнатуры тоже. Страж `lx-test/initerr/init_error_names_tag_lx_test.go` (`with_utls`), три кейса, red-check пройден. Условие снятия — в реестре [HOTFIXES](FEATURES/004-HOTFIXES/FEATURE.md): апстрим включит тип/тег в текст сам; файл апстримный и несёт швы других SPEC — маркер проверять на каждом мерже. [SPEC.md](TASKS/092-INIT_ERROR_NAMES_TAG/SPEC.md) |
| **093** | GRPC_SERVICE_NAME_CUSTOM_PATH | **I** | 🧵 **gRPC `service_name` в форме абсолютного пути — конвенция Xray custom path** (заявка лаунчера #130 от 2026-09-18; тот же класс жалоб разбирался 2026-09-03). Было: любое имя уходило на провод одним экранированным сегментом, `service_name: "a/b"` → `/a%2Fb/Tun`, а многосегментного пути не было вовсе — сервер Xray с `serviceName: "/a/b/Tun"` (документированная форма, сравнение пути строгое) отвечал `v2ray-grpc: unexpected status: 404 Not Found`, и узел был недостижим. Стало: **ведущий `/` = custom path** — сегменты экранируются по отдельности, последний сегмент = имя стрима (`/a/b/Stream` → сервис `a/b`, стрим `Stream`), хвост `\|…` (multi-стрим Xray) отбрасывается так же, как это делает Xray; имя **без** ведущего `/` ведёт себя байт в байт как раньше (`a/b` → `/a%2Fb/Tun`, = старая форма Xray). Форк-нативный helper `common/grpcname` — дословный порт `getServiceName`/`getTunStreamName` Xray, включая краевые случаи; правка в апстримных `transport/v2raygrpclite/{client,server}.go` и `transport/v2raygrpc/custom_name.go` (`// lx: SPEC 093`). Границы: новых опций и валидации нет — форму задаёт содержимое `service_name`, как у Xray; multi-стрим не реализуется; XHTTP `path`/`host` и прочие транспорты не затронуты; клиент под `with_grpc` для старой формы путь по-прежнему не экранирует (апстримное поведение, не меняли). Наш lite-сервер сравнивает **декодированный** путь, поэтому мягче Xray: сервер `a/b` принимает и `/a%2Fb/Tun`, и `/a/b/Tun`. Стражи `common/grpcname/grpcname_test.go` (табличный юнит §1.1), `transport/v2raygrpclite/service_name_lx_test.go` (h2-стенд lite client↔lite server) и `transport/v2raygrpc/custom_name_lx_test.go`, red-check пройден. Условие снятия — в реестре [HOTFIXES](FEATURES/004-HOTFIXES/FEATURE.md): апстрим примет конвенцию custom path (или эквивалент); файлы апстримные — маркер проверять на каждом мерже. Документация — [lx-protocols-transports.md §4.1](../docs-lx/lx-protocols-transports.md#41-service_name-the-xray-forms). ⚠️ Статус: **выпущено в `v1.14.1-lx.8`**; сверено с исходниками Xray и grpc-go, против живого Xray не прогонялось — ждём репортёра issue #130. [SPEC.md](TASKS/093-GRPC_SERVICE_NAME_CUSTOM_PATH/SPEC.md) |
| **073** | CHAIN_OUTBOUND | **I** | 🔗 **Outbound `chain` — виртуальная цепочка хопов из групп и узлов** (первая задача фичи [015-CHAIN](FEATURES/015-CHAIN/FEATURE.md); дизайн утверждён владельцем 2026-08-22, реализовано в тот же день: юниты + стенд `lx-test/chain` на живых ss-хопах зелёные). Позиции в порядке пакета (`[0]` вход → последний выход), любая позиция — узел/endpoint/группа; группы **не копируются** (хук в 5 точках дозвона групп подменяет выбранный узел на его рантайм-звено «узел через предыдущую позицию», созданное штатным реестром с `detour` на внутренний тег `<tag>#i`); звенья ленивые + прогрев детерминированных позиций, удаление по простою при нуле соединений, участвуют в ENERGY/quiesce; `direct` на позиции ≥1 прозрачен (выключатель хопа), `block` терминален; звено = оригинал − `strip` (каталог односторонних DPI-приёмов; `record_fragment` SPEC 060 не трогается) + `rewrite` по типу (merge-patch) с авто-понижением MTU туннелей на точные накладные IP-туннелей ниже; наблюдаемость: путь в `detourList`, `ChainInfo` в lx RPC/Clash API, задержка по слоям через URLTest `<tag>#i`. Build-tag `with_lx_chain`. [SPEC.md](TASKS/073-CHAIN_OUTBOUND/SPEC.md) |
| **068** | LXD_HOST_TELEMETRY | **C** | 🌡 **Телеметрия хоста, а не процесса.** `/admin/memory` показывает память демона; когда роутер на 256 МБ начинает тормозить, этого мало — оператор всё равно уходил по SSH за `top`/`free`/`df`. `GET /admin/host` отдаёт CPU (общий процент, **разрез по ядрам** — на четырёхъядерном MT7981 одно ядро в полке среднее прячет, `load_1/5/15`), память, термозоны, диски, дескрипторы; `GET /admin/host/interfaces` — интерфейсы с сырыми счётчиками и скоростями. Решения видно в ответе: **процент памяти от `available`, а не от `free`** (роутер держит почти всё в page cache — процент от free кричал бы «занято» при реально свободных 120 МБ); термозоны **массивом** + `max_celsius`, нет датчиков → `null`, а не пустой массив; диски массивом с `read_only`/`holds_state_dir`, раздел со state-dir ищется **по device id, а не по префиксу пути** (на OpenWrt `/etc` — симлинк в оверлей), `max_used_percent` игнорирует read-only ФС (вечно-100% squashfs делает индикатор бесполезным); дескрипторы **в двух уровнях** — свои и системные (разные баги с одним симптомом). Проценты и скорости — дельты, первый замер честно `null`; кеш 500 мс (замер на MT7981: цена TTL на CPU не влияет). **DEVICE-VERIFIED** на живом OpenWrt. [SPEC.md](TASKS/068-LXD_HOST_TELEMETRY/SPEC.md) |
| **067** | LXD_BUILD_TAG_SPLIT | **C** | 🔧 **Один тег гейтил две несвязанные вещи.** `with_lx_command` держал и расширения командного протокола libbox (`URLTestOutbound`/`GetRules`/`GetGroups` — ими живёт LxBox), и весь пакет `lxd/` с подкомандой демона: собрать одно без другого было нельзя. Теперь теги независимы — **`with_lx_command`** за RPC (`daemon/`, `experimental/libbox/`), **`with_lxd`** за демоном. Legacy-сборка Win7 идёт без демона (службы Windows и ротации лога там нет — подкоманда существовала бы без того, что делает её демоном), но с RPC: снять его значило бы молча сломать в LxBox тест задержки и экран правил ради 0.3 МБ из 46. AAR не затронут (gomobile собирает `experimental/libbox`, который `lxd/` не импортирует). Правки только в `//go:build`, `LX_TAGS` и наборах тегов CI — логика не менялась. [SPEC.md](TASKS/067-LXD_BUILD_TAG_SPLIT/SPEC.md) |
| **066** | LXD_CLIENT_IDENTITY | **D** | 🏷 **Справочник «IP → устройство» для сетевого инспектора** — соединение подписывается именем клиента и сегментом сети, а не голым адресом. `GET /admin/clients-info` отдаёт `name`/`mac`/`ssid`/`iface`/`port`/`source` по каждому клиенту; метки оператора — `PUT`/`DELETE` (ключ = IP или MAC). Пять провайдеров с приоритетом по порядку вызова (`lease` → `arp` → `bridge` → `wireless` → `label`), платформенные за build-тегами, ядро не трогается; кеш 60 с. **DEVICE-VERIFIED** на живом OpenWrt (MT7981), включая ubus/SSID и `/proc/net/arp`. [SPEC.md](TASKS/066-LXD_CLIENT_IDENTITY/SPEC.md) |
| **065** | LXD_OBSERVABILITY_PLANE | **C** | 👁 **Диагностика самого демона** — за тем же mTLS-пином, без отдельного debug-порта: `/admin/memory` (два RSS — текущий и пик, кеш 200 мс), `/admin/stats` (uptime ядра, трафик, соединения; без ядра `null`, а не 503 — «ядра нет» это состояние, а не ошибка), `/admin/logs` (хвост `lxd.log` — лог **демона**, которого в gRPC-потоке нет по построению), `/admin/pprof/*` (шесть снимков по whitelist, у CPU/trace потолок и 409 на параллельный забор, block/mutex включаются явно). Живой прогон на macOS: `go tool pprof` открывает и heap, и CPU-профиль с символизацией. [SPEC.md](TASKS/065-LXD_OBSERVABILITY_PLANE/SPEC.md) |
| **064** | SELECTOR_INTERRUPT_DEAD_ON_INBOUND | **I** | ✂️ **`interrupt_exist_connections` у `selector` не работал для пользовательского трафика** — апстримный дефект-ровесник самой фичи (2023-09-15, v1.10.0): переключение узла в группе не рвало активные соединения, они доживали на старом узле до своего таймаута, лечил только полный рестарт ядра. v1 (field-verified на rc.7) вытеснен **v2**: обёртка **входящего** conn вместо подмены `selected→s`, за счёт чего покрываются и вложенные группы. Полевой прогон v2 открыт. [SPEC.md](TASKS/064-SELECTOR_INTERRUPT_DEAD_ON_INBOUND/SPEC.md) |
| **063** | LXD_RESOURCE_STORE | **C** | 📦 **Вторая полезная нагрузка admin-плоскости: REST-CRUD над файловыми ресурсами**, на которые ссылается конфиг (`.srs` rule-set, geo-базы) — иначе `apply` мог сослаться на файл, которого на хосте нет. Адресация по имени + sha256 для дифа (клиент не льёт то, что уже лежит), гуард ссылок (409 на занятое имя — удалить ресурс из-под живого конфига нельзя), санитизация имён. Сквозное демо на macOS: реальный `.srs` → PUT → apply `type:local` → 409-гуард → освобождение → delete. [SPEC.md](TASKS/063-LXD_RESOURCE_STORE/SPEC.md) |
| **062** | MASQUE_CONFIG_SCHEMA_MIGRATION | **C** | 🔀 **Конфиг masque приведён к стандарту sing-box, со старой формой на алиасах.** Masque старше общих конвенций: версия HTTP жила в `network` (которое на любом другом outbound значит tcp/udp — ровно наоборот), а TLS-настройки были плоскими полями вместо стандартного блока. Теперь версия HTTP — **`vhttp`** (не `transport`: тот на vless/trojan/vmess объект, и переиспользование строкой повторило бы ту же путаницу), TLS — **штатный блок `tls`**, а значит masque даром получает всё, что даёт общий слой (в т.ч. авто-фрагментацию [060](TASKS/060-TLS_FRAGMENT_AUTO_ON_DETOUR/SPEC.md)). Старые `network`/`sni`/`skip_cert_verify`/`fragment*` работают до **v1.14.0-lx.30** с одним предупреждением на outbound; расхождение новой и старой формы — жёсткая ошибка, а не молчаливый выбор. ⚠️ Legacy-булевы неотличимы «не задано» от `false`, поэтому переносится только `true`. Заодно: дефолтный SNI больше не имя эндпоинта (его режет DPI), внятная ошибка когда endpoint молчит на CONNECT-IP. Обе схемы проверены на живом WARP. [SPEC.md](TASKS/062-MASQUE_CONFIG_SCHEMA_MIGRATION/SPEC.md) |
| **061** | XHTTP_DIAL_DOWNLOAD_DEADLOCK | **I** | 🔒 **Дедлок дайла `packet-up`/`stream-up` — баг фичи 002 с самого её появления.** Дайл ждал ответ на download-запрос внутри себя, а сервер придерживает нисходящий поток до первого uplink-пакета: клиент ждал сервер, сервер ждал клиента. Соединение обязано отдаваться вызывающему сразу. Корень воспроизведён на проводе, регрессионный тест зелёный; девайс-верификация открыта. [SPEC.md](TASKS/061-XHTTP_DIAL_DOWNLOAD_DEADLOCK/SPEC.md) |
| **060** | TLS_FRAGMENT_AUTO_ON_DETOUR | **C** | ✂️ **Связка «TLS-outbound + `detour`» молча не поднималась на части нижних плеч.** Нижнее плечо пересылает наш ClientHello от своего имени, а PMTU за тем сервером бывает ниже его размера; ICMP *Fragmentation Needed* до нас не доходит ⇒ пакет исчезает, снаружи `tls handshake: EOF` через 12–17 с. Порог ≈1490 B — свойство **пути**, не протокола: воспроизводится голым `curl` без sing-box, а `mtu`/`tun.mtu`/route-action `tls_fragment` до этого места не достают (ClientHello рождается ДО туннеля и мимо роутера). Решение: под `detour` **`record_fragment` включается по умолчанию** — одной точкой в `common/tls`, поэтому STD, uTLS и REALITY получают одинаковый дефолт, а не «помни, где ставить флаг». Замер через сломанное плечо: без фрагментации ❌ 12 с, `fragment` ✅ 0.6 с, `record_fragment` ✅ **0.1 с** (не ждёт ACK, который через detour всё равно недоступен). Явное значение всегда сильнее; `fragment: true` не апгрейдится; h3/QUIC не затронут. ⚠️ Известный компромисс: явный `record_fragment: false` неотличим от «не задано» — «detour без всякой фрагментации» сейчас не выразить. Матрица 38/38 на живых узлах. [SPEC.md](TASKS/060-TLS_FRAGMENT_AUTO_ON_DETOUR/SPEC.md) |
| **059** | XHTTP_XMUX | **I** | 🔗 **Переиспользование HTTP-соединений XHTTP (`xmux`)** — цель двойная: **совместимость** с Xray-серверами (секция `xmux` приходит в конфигах из подписок, сейчас молча игнорируется — клиент ведёт себя не так, как задумал владелец сервера) и **оптимизация** (без пула каждый поток платит полный TCP+TLS(+REALITY) хендшейк). Модель референса [sing-box-extended `mux.go`](https://github.com/shtorm-7/sing-box-extended/blob/v1.13.18-extended-2.6.4/transport/v2rayxhttp/mux.go) (слепок приложен в задаче) поверх Xray `XmuxConfig`: пул соединений с вытеснением по четырём критериям (закрыт / `c_max_reuse_times` / `h_max_request_times` / `h_max_reusable_secs`), случайный выбор кандидата, **отложенное закрытие** при живых потоках. Диапазоны — наша строковая форма `"min-max"` + приём `[min,max]` и числа (конфиги из подписок). Дефолты применяются и без секции (`1-1` / `600-900` / `1800-3000`). Тонкие места: `leftRequests` считает **HTTP-запросы**, а не потоки (packet-up = много POST на поток); декремент `openUsage` обязан быть идемпотентным (двойной `Close()` — реальность SPEC 050); счётчик держится до закрытия обеих половин асинхронного download (`ead099674`). Client-only: серверная часть и секция `download` — вне объёма. [SPEC.md](TASKS/059-XHTTP_XMUX/SPEC.md) |
| **058** | GET_URL_VIA_OUTBOUND | **C** | 🌐 **HTTP-пробник узла: GET по произвольному URL через outbound/endpoint по тегу с возвратом тела ответа** — диагностика следующего класса после «жив/мёртв»: exit-IP, гео, `warp=` (`1.1.1.1/cdn-cgi/trace`, ip2location) из CC без переключения активного selector'а. Синхронный unary; ответ = статус+тело (кламп 256 KiB, потолок 1 MiB, признак обрезки)+Content-Type+remoteAddr+elapsed; **не-2xx — результат, а не ошибка**; GET-only (SSRF-граница), headers опциональны (nullable-билдер — gomobile не умеет map); историю urltest не трогает. Шов = `URLTestOutbound` (резолв тега в обоих менеджерах, отмена per-call ctx); возврат объектом (SPEC 038). [SPEC.md](TASKS/058-GET_URL_VIA_OUTBOUND/SPEC.md) |
| **057** | LXD_MTLS_SERVICE | **C** | 🔐 **mTLS-канал, регистрация клиентов и системная служба для демона `lxd`** (CLI-контракт владельца 2026-08-08: демон = голая команда `sing-box lxd`, не `run`). Демон сам себе CA (`--tls`): на первом старте самоподписывает серверную пару (стабильный отпечаток в state-dir), при нуле клиентов печатает приглашение `адрес#отпечаток#код`. Лаунчер пинит сервер по отпечатку, генерит свою пару, `POST /admin/enroll` по **одноразовому коду** (без TTL — не подобрать) → сервер пинит клиентский серт; клиентский пин на **обеих** плоскостях (admin-REST и gRPC-наблюдение), enroll — единственный маршрут до доверия. `client add/list/remove` (модель WG-пиров). `/admin/start`+`/admin/stop` — жизнь ядра отдельно от конфига; память `was_running` восстанавливает состояние после рестарта (модель boxdd). `--config-force`/`--run`; connection-настройки — только `<state-dir>/daemon.json` (0600, флагов нет по построению), там же лимиты ротации лога. Демон владеет `lxd.log`: под службой перехватывает stdout/stderr (включая лог ядра и паники) и ротирует — дефолты «≈сутки истории» (24 ч, 1 бэкап, 20 МБ). `GET /admin/info` — паспорт демона (версия, state_dir, listen, отпечаток, uptime, log_path). Служба: macOS `--service=install` / `install-user` ставят по-настоящему (сами материализуют daemon.json, plist с абсолютными путями — относительный state-dir крэш-циклил под launchd, пойман и починен); **linux — только печать рецепта** (принцип: диск меняет оператор) с детектом init по `/proc/1/comm`; `--dry-run` вместо режима `print`; windows — заглушка. Device-verified на macOS: enrollment, обе роли службы (user без sudo + system под sudo), KeepAlive-перезапуск, purge. [SPEC.md](TASKS/057-LXD_MTLS_SERVICE/SPEC.md) |
| **056** | LXD_APPLY_ROLLBACK | **C** | 🛡️ **Admin-плоскость MVP демона `lxd`** (замечание владельца: демон стартует и вовсе без конфига — IDLE, ядро оживает первым apply). Один порт — две плоскости: h2c-мультиплексор шлёт `application/grpc` в `daemon.StartedService`, остальное — в admin-REST (stdlib-клиент лаунчера, win7 ok; Bearer constant-time). `POST /admin/apply`: кандидат на диск → валидация **сабпроцессом** `check` (крэш-изолирована, 422 = инстанс не тронут — «проверь до убийства») → подмена инстанса → last-good только после успешного старта; провал Start (bind/tun) = **автооткат** на last-good (демо: 500 `rolled_back:true`, статус `started`, причина в `last_error`). Прерванный apply помечается, кандидат не бутится никогда; рестарт демона сам поднимается из last-good. `/admin/rollback|config|status`. SIGHUP — тот же пайплайн. Впереди: confirm/dead-man, история версий, SRS, mTLS. [SPEC.md](TASKS/056-LXD_APPLY_ROLLBACK/SPEC.md) |
| **055** | LXD_DAEMON_SKELETON | **C** | 🛰️ **Скелет headless-демона `sing-box lxd`** (в задаче — `lxd run`, синтаксис изменён в 057) (первая задача фичи [014-LXD_DAEMON](FEATURES/014-LXD_DAEMON/FEATURE.md), решение владельца 2026-08-07): ядро живёт in-process за gRPC-каналом `daemon.StartedService`, который принадлежит процессу и переживает смену конфига — SIGHUP подменяет box-инстанс под живым сервером, битый конфиг оставляет демон в FATAL с работающим каналом (управление доступно, когда data-plane лежит). Сабкоманда самонавешивается собственным файлом — ноль правок апстримных файлов; один артефакт с ядром (extractor лаунчера не трогается). Демо на macOS: один стрим статусов без переподключения увидел reload → FATAL → восстановление. Впереди (дорожная карта фичи): admin-плоскость apply/rollback/версии, SRS, mTLS, reverse-proxy Clash API, `service install`. [SPEC.md](TASKS/055-LXD_DAEMON_SKELETON/SPEC.md) |
| **054** | URLTEST_PENALTY_FAILOVER | **C** | 🎯 **least_test-группа теперь реагирует на отказы боевых дайлов** (потребитель 15с-ошибок SPEC 052; дизайн владельца). Отказ «путь мёртв» (DeadlineExceeded/unreachable — НЕ RST и НЕ Canceled) → узлу +1 штраф и ОДИН fallback-дайл через лучшего кандидата (кап 2 попытки, худший случай 30с); успех переносит выбор без `Interrupt`. Аварийный режим (у лучшего-по-скорости ≥3): выбор ранжируется двумя уровнями — штрафы ↑, затем задержка ↑; победители по штрафам соревнуются по скорости. Сброс штрафа ТОЛЬКО доказательством жизни (успешный дайл / ответ на пробу) — никаких сбросов по времени. Тотальные штрафы → force-прогон: уровень-триггер, не чаще раза в 2 мин **от конца прошлого прогона**; passive-skip в аварийном режиме отключён (иначе бывший лучший никогда не получил бы сбрасывающую пробу). Ни одного нового таймера — все проверки дельтами в момент события. round_robin/UDP-штрафы/дедуп по назначениям — сознательно вне (см. SPEC). [SPEC.md](TASKS/054-URLTEST_PENALTY_FAILOVER/SPEC.md) |

> **Вне этого репозитория:** потребление ядра лаунчером (`singbox-launcher`) — парсинг `type=xhttp` в реальный XHTTP-транспорт (сейчас `023` маппит его в `httpupgrade`), AWG-поля в визарде, замена `bin/sing-box`. Это отдельные задачи в репозитории лаунчера.
